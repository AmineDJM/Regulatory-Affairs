# Généré par npx tsx scripts/api/gen-docs.ts — ne pas modifier à la main.
"openapi": "3.1.0"
"info": 
  "title": "AMD Internal OS — API pour agents"
  "version": "1.0.0"
  "description": |-
      API machine de l'ERP interne d'Adventum Pharma (groupe pharmaceutique algérien).
      
      **Deux couches d'autorisation, toujours cumulées.** Les *portées* (scopes) disent ce que l'intégration a le droit de faire ; l'*identité* au nom de laquelle l'agent agit dit ce qu'elle voit dans l'ERP. Un agent ne dépasse jamais ni l'une ni l'autre.
      
      **L'API n'a pas de logique propre.** Elle appelle les mêmes fonctions métier et les mêmes gardes que les écrans : ce qui est interdit à l'écran l'est ici, et pour la même raison.
      
      **Par où commencer** : `list_modules` (ce que je peux), puis `search_erp` (trouver), puis `get_{objet}_workflow` (où en est-on, qu'est-ce qui bloque).
  "contact": 
    "name": "Adventum Pharma — Administration AMD Internal OS"
"servers": 
  -     "url": "https://amd-internal-os.onrender.com"
    "description": "Production"
"paths": 
  "/api/v1/meta/modules": 
    "get": 
      "operationId": "list_modules"
      "summary": "Lister les modules de l'ERP et ce que cette identité peut y faire"
      "description": "Point de départ d'un agent. Rend les modules réels, les droits EFFECTIFS de l'identité au nom de laquelle l'agent agit (action par action), les portées accordées au client, et les objets exposés par module."
      "tags": 
        - "Découverte"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "responses": 
        "200": 
          "description": "Modules et droits effectifs."
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/meta/entities": 
    "get": 
      "operationId": "list_entities"
      "summary": "Lister les objets métier exposés"
      "description": "Catalogue des objets, leur module, leur description métier et le droit de lecture réel de l'identité."
      "tags": 
        - "Découverte"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "responses": 
        "200": 
          "description": "Catalogue des objets."
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/meta/entities/{entity}": 
    "get": 
      "operationId": "get_entity_schema"
      "summary": "Décrire un objet : champs, types, énumérations, relations, opérations"
      "description": "Schéma lu dans la base au moment de la demande — donc toujours exact. Dit aussi quels champs sont filtrables, lesquels sont cherchables, et quelles opérations existent sur cet objet."
      "tags": 
        - "Découverte"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "entity"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
            "enum": 
              - "regulatory_dossier"
              - "supplier"
              - "company"
              - "department"
              - "employee"
              - "leave_request"
              - "admin_request"
              - "sponsoring"
              - "congress_international"
              - "congress_national"
              - "event"
              - "expense_order"
              - "department_budget"
              - "department_expense"
              - "stock_snapshot"
              - "pch_tender"
              - "doctor"
              - "medical_visit"
              - "medical_info_declaration"
              - "sale"
              - "dossier"
              - "task"
              - "document"
              - "user"
              - "audit_log"
              - "notification"
      "responses": 
        "200": 
          "description": "Schéma de l'objet."
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/search": 
    "get": 
      "operationId": "search_erp"
      "summary": "Recherche globale dans tout l'ERP"
      "description": "Cherche le texte dans tous les objets que l'identité a le droit de lire, chacun avec sa portée. Rend, par objet, les correspondances avec leur titre, leur statut et le chemin pour aller plus loin."
      "tags": 
        - "Recherche"
      "security": 
        -           "apiKey": 
            - "erp.search"
      "parameters": 
        -           "name": "q"
          "in": "query"
          "required": true
          "schema": 
            "type": "string"
            "minLength": 2
          "description": "Texte cherché (2 caractères minimum)."
        -           "name": "entities"
          "in": "query"
          "schema": 
            "type": "string"
          "description": "Restreindre à certains objets, séparés par des virgules."
        -           "name": "from"
          "in": "query"
          "schema": 
            "type": "string"
            "format": "date"
          "description": "Créés à partir de cette date."
        -           "name": "to"
          "in": "query"
          "schema": 
            "type": "string"
            "format": "date"
          "description": "Créés jusqu'à cette date."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Résultats groupés par objet."
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/regulatory_dossier": 
    "get": 
      "operationId": "list_regulatory_dossier"
      "summary": "Lister : Dossier réglementaire"
      "description": |-
          Un produit (DCI, dosage, forme, conditionnement) suivi jusqu'à sa décision d'enregistrement (DE) auprès de l'ANPP. C'est l'objet central du module Regulatory : il porte le workflow en 17 étapes, le processus officiel ANPP, les variations de fabrication, les pièces et les demandes de bon de virement.
          
          Filtres possibles sur : id, reference, dci, brandName, dosage, dosageUnit, pharmaceuticalForm, packaging, status, priority, manufacturingStatus, responsibleId, companyId, targetDate, updatedAt, therapeuticClass, comments. Syntaxe : `champ=valeur`, `champ=in:A,B`, `champ=gte:2026-01-01`, `champ=contains:texte`, `champ=null`.
      "tags": 
        - "REGULATORY"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "q"
          "in": "query"
          "schema": 
            "type": "string"
          "description": "Recherche plein texte sur : reference, dci, brandName, therapeuticClass, comments."
        -           "name": "sort"
          "in": "query"
          "schema": 
            "type": "string"
          "description": "Tri « champ:asc » ou « champ:desc »."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Liste paginée de dossier réglementaire."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/ListResult"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/regulatory_dossier/{id}": 
    "get": 
      "operationId": "get_regulatory_dossier"
      "summary": "Lire un dossier réglementaire"
      "description": "Un produit (DCI, dosage, forme, conditionnement) suivi jusqu'à sa décision d'enregistrement (DE) auprès de l'ANPP. C'est l'objet central du module Regulatory : il porte le workflow en 17 étapes, le processus officiel ANPP, les variations de fabrication, les pièces et les demandes de bon de virement. Rend la fiche complète et les liens vers ses facettes (historique, pièces, circuit, actions)."
      "tags": 
        - "REGULATORY"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du dossier réglementaire."
      "responses": 
        "200": 
          "description": "Fiche complète."
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/regulatory_dossier/{id}/history": 
    "get": 
      "operationId": "get_regulatory_dossier_history"
      "summary": "Historique — actions humaines ET appels d'agents, distingués"
      "description": "Historique — actions humaines ET appels d'agents, distingués"
      "tags": 
        - "REGULATORY"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du dossier réglementaire."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Historique — actions humaines ET appels d'agents, distingués"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/regulatory_dossier/{id}/documents": 
    "get": 
      "operationId": "list_regulatory_dossier_documents"
      "summary": "Pièces jointes, avec leur chemin de téléchargement contrôlé"
      "description": "Pièces jointes, avec leur chemin de téléchargement contrôlé"
      "tags": 
        - "REGULATORY"
      "security": 
        -           "apiKey": 
            - "erp.documents.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du dossier réglementaire."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Pièces jointes, avec leur chemin de téléchargement contrôlé"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/regulatory_dossier/{id}/comments": 
    "get": 
      "operationId": "list_regulatory_dossier_comments"
      "summary": "Commentaires portés sur l'objet"
      "description": "Commentaires portés sur l'objet"
      "tags": 
        - "REGULATORY"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du dossier réglementaire."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Commentaires portés sur l'objet"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/regulatory_dossier/{id}/related": 
    "get": 
      "operationId": "get_regulatory_dossier_related"
      "summary": "Objets liés (sous-objets, lignes, participants)"
      "description": "Objets liés (sous-objets, lignes, participants)"
      "tags": 
        - "REGULATORY"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du dossier réglementaire."
      "responses": 
        "200": 
          "description": "Objets liés (sous-objets, lignes, participants)"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/regulatory_dossier/{id}/workflow": 
    "get": 
      "operationId": "get_regulatory_dossier_workflow"
      "summary": "Circuit : étapes, étape courante, responsable, échéances, BLOCAGES"
      "description": "Représentation structurée du circuit : étapes du workflow, celles déjà réalisées, l'étape courante, les prochaines possibles, le responsable, les dates et échéances, les pièces manquantes et les conditions à réunir pour avancer. C'est la réponse à « qu'est-ce qui bloque ce dossier ? »."
      "tags": 
        - "REGULATORY"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du dossier réglementaire."
      "responses": 
        "200": 
          "description": "Circuit : étapes, étape courante, responsable, échéances, BLOCAGES"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/regulatory_dossier/{id}/available-actions": 
    "get": 
      "operationId": "get_regulatory_dossier_available_actions"
      "summary": "Ce que l'agent peut faire MAINTENANT sur cet objet, et pourquoi pas le reste"
      "description": "Ce que l'agent peut faire MAINTENANT sur cet objet, et pourquoi pas le reste"
      "tags": 
        - "REGULATORY"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du dossier réglementaire."
      "responses": 
        "200": 
          "description": "Ce que l'agent peut faire MAINTENANT sur cet objet, et pourquoi pas le reste"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/supplier": 
    "get": 
      "operationId": "list_supplier"
      "summary": "Lister : Fournisseur / laboratoire partenaire"
      "description": |-
          Laboratoire ou fournisseur rattaché à des dossiers réglementaires ; peut disposer d'un accès au portail externe.
          
          Filtres possibles sur : id, name, country, contactEmail, active, createdAt, notes. Syntaxe : `champ=valeur`, `champ=in:A,B`, `champ=gte:2026-01-01`, `champ=contains:texte`, `champ=null`.
      "tags": 
        - "REGULATORY"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "q"
          "in": "query"
          "schema": 
            "type": "string"
          "description": "Recherche plein texte sur : name, country, contactEmail, notes."
        -           "name": "sort"
          "in": "query"
          "schema": 
            "type": "string"
          "description": "Tri « champ:asc » ou « champ:desc »."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Liste paginée de fournisseur / laboratoire partenaire."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/ListResult"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/supplier/{id}": 
    "get": 
      "operationId": "get_supplier"
      "summary": "Lire un fournisseur / laboratoire partenaire"
      "description": "Laboratoire ou fournisseur rattaché à des dossiers réglementaires ; peut disposer d'un accès au portail externe. Rend la fiche complète et les liens vers ses facettes (historique, pièces, circuit, actions)."
      "tags": 
        - "REGULATORY"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du fournisseur / laboratoire partenaire."
      "responses": 
        "200": 
          "description": "Fiche complète."
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/supplier/{id}/history": 
    "get": 
      "operationId": "get_supplier_history"
      "summary": "Historique — actions humaines ET appels d'agents, distingués"
      "description": "Historique — actions humaines ET appels d'agents, distingués"
      "tags": 
        - "REGULATORY"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du fournisseur / laboratoire partenaire."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Historique — actions humaines ET appels d'agents, distingués"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/supplier/{id}/documents": 
    "get": 
      "operationId": "list_supplier_documents"
      "summary": "Pièces jointes, avec leur chemin de téléchargement contrôlé"
      "description": "Pièces jointes, avec leur chemin de téléchargement contrôlé"
      "tags": 
        - "REGULATORY"
      "security": 
        -           "apiKey": 
            - "erp.documents.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du fournisseur / laboratoire partenaire."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Pièces jointes, avec leur chemin de téléchargement contrôlé"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/supplier/{id}/comments": 
    "get": 
      "operationId": "list_supplier_comments"
      "summary": "Commentaires portés sur l'objet"
      "description": "Commentaires portés sur l'objet"
      "tags": 
        - "REGULATORY"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du fournisseur / laboratoire partenaire."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Commentaires portés sur l'objet"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/supplier/{id}/related": 
    "get": 
      "operationId": "get_supplier_related"
      "summary": "Objets liés (sous-objets, lignes, participants)"
      "description": "Objets liés (sous-objets, lignes, participants)"
      "tags": 
        - "REGULATORY"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du fournisseur / laboratoire partenaire."
      "responses": 
        "200": 
          "description": "Objets liés (sous-objets, lignes, participants)"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/supplier/{id}/workflow": 
    "get": 
      "operationId": "get_supplier_workflow"
      "summary": "Circuit : étapes, étape courante, responsable, échéances, BLOCAGES"
      "description": "Représentation structurée du circuit : étapes du workflow, celles déjà réalisées, l'étape courante, les prochaines possibles, le responsable, les dates et échéances, les pièces manquantes et les conditions à réunir pour avancer. C'est la réponse à « qu'est-ce qui bloque ce dossier ? »."
      "tags": 
        - "REGULATORY"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du fournisseur / laboratoire partenaire."
      "responses": 
        "200": 
          "description": "Circuit : étapes, étape courante, responsable, échéances, BLOCAGES"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/supplier/{id}/available-actions": 
    "get": 
      "operationId": "get_supplier_available_actions"
      "summary": "Ce que l'agent peut faire MAINTENANT sur cet objet, et pourquoi pas le reste"
      "description": "Ce que l'agent peut faire MAINTENANT sur cet objet, et pourquoi pas le reste"
      "tags": 
        - "REGULATORY"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du fournisseur / laboratoire partenaire."
      "responses": 
        "200": 
          "description": "Ce que l'agent peut faire MAINTENANT sur cet objet, et pourquoi pas le reste"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/company": 
    "get": 
      "operationId": "list_company"
      "summary": "Lister : Entité du groupe"
      "description": |-
          Société du groupe (Adventum, Pharmagène…). L'entité cloisonne la plupart des objets de la plateforme.
          
          Filtres possibles sur : id, name, shortName, color, isActive, sortOrder. Syntaxe : `champ=valeur`, `champ=in:A,B`, `champ=gte:2026-01-01`, `champ=contains:texte`, `champ=null`.
      "tags": 
        - "DASHBOARD"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "q"
          "in": "query"
          "schema": 
            "type": "string"
          "description": "Recherche plein texte sur : name, shortName."
        -           "name": "sort"
          "in": "query"
          "schema": 
            "type": "string"
          "description": "Tri « champ:asc » ou « champ:desc »."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Liste paginée de entité du groupe."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/ListResult"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/company/{id}": 
    "get": 
      "operationId": "get_company"
      "summary": "Lire un entité du groupe"
      "description": "Société du groupe (Adventum, Pharmagène…). L'entité cloisonne la plupart des objets de la plateforme. Rend la fiche complète et les liens vers ses facettes (historique, pièces, circuit, actions)."
      "tags": 
        - "DASHBOARD"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du entité du groupe."
      "responses": 
        "200": 
          "description": "Fiche complète."
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/company/{id}/history": 
    "get": 
      "operationId": "get_company_history"
      "summary": "Historique — actions humaines ET appels d'agents, distingués"
      "description": "Historique — actions humaines ET appels d'agents, distingués"
      "tags": 
        - "DASHBOARD"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du entité du groupe."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Historique — actions humaines ET appels d'agents, distingués"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/company/{id}/documents": 
    "get": 
      "operationId": "list_company_documents"
      "summary": "Pièces jointes, avec leur chemin de téléchargement contrôlé"
      "description": "Pièces jointes, avec leur chemin de téléchargement contrôlé"
      "tags": 
        - "DASHBOARD"
      "security": 
        -           "apiKey": 
            - "erp.documents.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du entité du groupe."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Pièces jointes, avec leur chemin de téléchargement contrôlé"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/company/{id}/comments": 
    "get": 
      "operationId": "list_company_comments"
      "summary": "Commentaires portés sur l'objet"
      "description": "Commentaires portés sur l'objet"
      "tags": 
        - "DASHBOARD"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du entité du groupe."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Commentaires portés sur l'objet"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/company/{id}/related": 
    "get": 
      "operationId": "get_company_related"
      "summary": "Objets liés (sous-objets, lignes, participants)"
      "description": "Objets liés (sous-objets, lignes, participants)"
      "tags": 
        - "DASHBOARD"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du entité du groupe."
      "responses": 
        "200": 
          "description": "Objets liés (sous-objets, lignes, participants)"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/company/{id}/workflow": 
    "get": 
      "operationId": "get_company_workflow"
      "summary": "Circuit : étapes, étape courante, responsable, échéances, BLOCAGES"
      "description": "Représentation structurée du circuit : étapes du workflow, celles déjà réalisées, l'étape courante, les prochaines possibles, le responsable, les dates et échéances, les pièces manquantes et les conditions à réunir pour avancer. C'est la réponse à « qu'est-ce qui bloque ce dossier ? »."
      "tags": 
        - "DASHBOARD"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du entité du groupe."
      "responses": 
        "200": 
          "description": "Circuit : étapes, étape courante, responsable, échéances, BLOCAGES"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/company/{id}/available-actions": 
    "get": 
      "operationId": "get_company_available_actions"
      "summary": "Ce que l'agent peut faire MAINTENANT sur cet objet, et pourquoi pas le reste"
      "description": "Ce que l'agent peut faire MAINTENANT sur cet objet, et pourquoi pas le reste"
      "tags": 
        - "DASHBOARD"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du entité du groupe."
      "responses": 
        "200": 
          "description": "Ce que l'agent peut faire MAINTENANT sur cet objet, et pourquoi pas le reste"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/department": 
    "get": 
      "operationId": "list_department"
      "summary": "Lister : Département"
      "description": |-
          Nœud de l'organigramme (N niveaux). Porte les budgets, la caisse d'avance et la chaîne du N+1.
          
          Filtres possibles sur : id, name, code, parentId, companyId, headId, deputyId, description. Syntaxe : `champ=valeur`, `champ=in:A,B`, `champ=gte:2026-01-01`, `champ=contains:texte`, `champ=null`.
      "tags": 
        - "RH"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "q"
          "in": "query"
          "schema": 
            "type": "string"
          "description": "Recherche plein texte sur : name, code."
        -           "name": "sort"
          "in": "query"
          "schema": 
            "type": "string"
          "description": "Tri « champ:asc » ou « champ:desc »."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Liste paginée de département."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/ListResult"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/department/{id}": 
    "get": 
      "operationId": "get_department"
      "summary": "Lire un département"
      "description": "Nœud de l'organigramme (N niveaux). Porte les budgets, la caisse d'avance et la chaîne du N+1. Rend la fiche complète et les liens vers ses facettes (historique, pièces, circuit, actions)."
      "tags": 
        - "RH"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du département."
      "responses": 
        "200": 
          "description": "Fiche complète."
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/department/{id}/history": 
    "get": 
      "operationId": "get_department_history"
      "summary": "Historique — actions humaines ET appels d'agents, distingués"
      "description": "Historique — actions humaines ET appels d'agents, distingués"
      "tags": 
        - "RH"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du département."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Historique — actions humaines ET appels d'agents, distingués"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/department/{id}/documents": 
    "get": 
      "operationId": "list_department_documents"
      "summary": "Pièces jointes, avec leur chemin de téléchargement contrôlé"
      "description": "Pièces jointes, avec leur chemin de téléchargement contrôlé"
      "tags": 
        - "RH"
      "security": 
        -           "apiKey": 
            - "erp.documents.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du département."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Pièces jointes, avec leur chemin de téléchargement contrôlé"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/department/{id}/comments": 
    "get": 
      "operationId": "list_department_comments"
      "summary": "Commentaires portés sur l'objet"
      "description": "Commentaires portés sur l'objet"
      "tags": 
        - "RH"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du département."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Commentaires portés sur l'objet"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/department/{id}/related": 
    "get": 
      "operationId": "get_department_related"
      "summary": "Objets liés (sous-objets, lignes, participants)"
      "description": "Objets liés (sous-objets, lignes, participants)"
      "tags": 
        - "RH"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du département."
      "responses": 
        "200": 
          "description": "Objets liés (sous-objets, lignes, participants)"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/department/{id}/workflow": 
    "get": 
      "operationId": "get_department_workflow"
      "summary": "Circuit : étapes, étape courante, responsable, échéances, BLOCAGES"
      "description": "Représentation structurée du circuit : étapes du workflow, celles déjà réalisées, l'étape courante, les prochaines possibles, le responsable, les dates et échéances, les pièces manquantes et les conditions à réunir pour avancer. C'est la réponse à « qu'est-ce qui bloque ce dossier ? »."
      "tags": 
        - "RH"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du département."
      "responses": 
        "200": 
          "description": "Circuit : étapes, étape courante, responsable, échéances, BLOCAGES"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/department/{id}/available-actions": 
    "get": 
      "operationId": "get_department_available_actions"
      "summary": "Ce que l'agent peut faire MAINTENANT sur cet objet, et pourquoi pas le reste"
      "description": "Ce que l'agent peut faire MAINTENANT sur cet objet, et pourquoi pas le reste"
      "tags": 
        - "RH"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du département."
      "responses": 
        "200": 
          "description": "Ce que l'agent peut faire MAINTENANT sur cet objet, et pourquoi pas le reste"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/employee": 
    "get": 
      "operationId": "list_employee"
      "summary": "Lister : Employé"
      "description": |-
          Fiche du personnel : contrat, département, congés, paie, documents.
          
          Filtres possibles sur : id, fullName, position, departmentId, companyId, isActive, hireDate, contractType, userId, email, notes. Syntaxe : `champ=valeur`, `champ=in:A,B`, `champ=gte:2026-01-01`, `champ=contains:texte`, `champ=null`.
      "tags": 
        - "RH"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "q"
          "in": "query"
          "schema": 
            "type": "string"
          "description": "Recherche plein texte sur : fullName, position, email, notes."
        -           "name": "sort"
          "in": "query"
          "schema": 
            "type": "string"
          "description": "Tri « champ:asc » ou « champ:desc »."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Liste paginée de employé."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/ListResult"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/employee/{id}": 
    "get": 
      "operationId": "get_employee"
      "summary": "Lire un employé"
      "description": "Fiche du personnel : contrat, département, congés, paie, documents. Rend la fiche complète et les liens vers ses facettes (historique, pièces, circuit, actions)."
      "tags": 
        - "RH"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du employé."
      "responses": 
        "200": 
          "description": "Fiche complète."
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/employee/{id}/history": 
    "get": 
      "operationId": "get_employee_history"
      "summary": "Historique — actions humaines ET appels d'agents, distingués"
      "description": "Historique — actions humaines ET appels d'agents, distingués"
      "tags": 
        - "RH"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du employé."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Historique — actions humaines ET appels d'agents, distingués"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/employee/{id}/documents": 
    "get": 
      "operationId": "list_employee_documents"
      "summary": "Pièces jointes, avec leur chemin de téléchargement contrôlé"
      "description": "Pièces jointes, avec leur chemin de téléchargement contrôlé"
      "tags": 
        - "RH"
      "security": 
        -           "apiKey": 
            - "erp.documents.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du employé."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Pièces jointes, avec leur chemin de téléchargement contrôlé"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/employee/{id}/comments": 
    "get": 
      "operationId": "list_employee_comments"
      "summary": "Commentaires portés sur l'objet"
      "description": "Commentaires portés sur l'objet"
      "tags": 
        - "RH"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du employé."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Commentaires portés sur l'objet"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/employee/{id}/related": 
    "get": 
      "operationId": "get_employee_related"
      "summary": "Objets liés (sous-objets, lignes, participants)"
      "description": "Objets liés (sous-objets, lignes, participants)"
      "tags": 
        - "RH"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du employé."
      "responses": 
        "200": 
          "description": "Objets liés (sous-objets, lignes, participants)"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/employee/{id}/workflow": 
    "get": 
      "operationId": "get_employee_workflow"
      "summary": "Circuit : étapes, étape courante, responsable, échéances, BLOCAGES"
      "description": "Représentation structurée du circuit : étapes du workflow, celles déjà réalisées, l'étape courante, les prochaines possibles, le responsable, les dates et échéances, les pièces manquantes et les conditions à réunir pour avancer. C'est la réponse à « qu'est-ce qui bloque ce dossier ? »."
      "tags": 
        - "RH"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du employé."
      "responses": 
        "200": 
          "description": "Circuit : étapes, étape courante, responsable, échéances, BLOCAGES"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/employee/{id}/available-actions": 
    "get": 
      "operationId": "get_employee_available_actions"
      "summary": "Ce que l'agent peut faire MAINTENANT sur cet objet, et pourquoi pas le reste"
      "description": "Ce que l'agent peut faire MAINTENANT sur cet objet, et pourquoi pas le reste"
      "tags": 
        - "RH"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du employé."
      "responses": 
        "200": 
          "description": "Ce que l'agent peut faire MAINTENANT sur cet objet, et pourquoi pas le reste"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/leave_request": 
    "get": 
      "operationId": "list_leave_request"
      "summary": "Lister : Demande de congé"
      "description": |-
          Congé soumis à la chaîne de validation à trois étages : responsable → ressources humaines → direction.
          
          Filtres possibles sur : id, employeeId, type, startDate, endDate, days, status, stage, managerId, createdAt, reason. Syntaxe : `champ=valeur`, `champ=in:A,B`, `champ=gte:2026-01-01`, `champ=contains:texte`, `champ=null`.
      "tags": 
        - "RH"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "q"
          "in": "query"
          "schema": 
            "type": "string"
          "description": "Recherche plein texte sur : reason."
        -           "name": "sort"
          "in": "query"
          "schema": 
            "type": "string"
          "description": "Tri « champ:asc » ou « champ:desc »."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Liste paginée de demande de congé."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/ListResult"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/leave_request/{id}": 
    "get": 
      "operationId": "get_leave_request"
      "summary": "Lire un demande de congé"
      "description": "Congé soumis à la chaîne de validation à trois étages : responsable → ressources humaines → direction. Rend la fiche complète et les liens vers ses facettes (historique, pièces, circuit, actions)."
      "tags": 
        - "RH"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du demande de congé."
      "responses": 
        "200": 
          "description": "Fiche complète."
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/leave_request/{id}/history": 
    "get": 
      "operationId": "get_leave_request_history"
      "summary": "Historique — actions humaines ET appels d'agents, distingués"
      "description": "Historique — actions humaines ET appels d'agents, distingués"
      "tags": 
        - "RH"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du demande de congé."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Historique — actions humaines ET appels d'agents, distingués"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/leave_request/{id}/documents": 
    "get": 
      "operationId": "list_leave_request_documents"
      "summary": "Pièces jointes, avec leur chemin de téléchargement contrôlé"
      "description": "Pièces jointes, avec leur chemin de téléchargement contrôlé"
      "tags": 
        - "RH"
      "security": 
        -           "apiKey": 
            - "erp.documents.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du demande de congé."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Pièces jointes, avec leur chemin de téléchargement contrôlé"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/leave_request/{id}/comments": 
    "get": 
      "operationId": "list_leave_request_comments"
      "summary": "Commentaires portés sur l'objet"
      "description": "Commentaires portés sur l'objet"
      "tags": 
        - "RH"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du demande de congé."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Commentaires portés sur l'objet"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/leave_request/{id}/related": 
    "get": 
      "operationId": "get_leave_request_related"
      "summary": "Objets liés (sous-objets, lignes, participants)"
      "description": "Objets liés (sous-objets, lignes, participants)"
      "tags": 
        - "RH"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du demande de congé."
      "responses": 
        "200": 
          "description": "Objets liés (sous-objets, lignes, participants)"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/leave_request/{id}/workflow": 
    "get": 
      "operationId": "get_leave_request_workflow"
      "summary": "Circuit : étapes, étape courante, responsable, échéances, BLOCAGES"
      "description": "Représentation structurée du circuit : étapes du workflow, celles déjà réalisées, l'étape courante, les prochaines possibles, le responsable, les dates et échéances, les pièces manquantes et les conditions à réunir pour avancer. C'est la réponse à « qu'est-ce qui bloque ce dossier ? »."
      "tags": 
        - "RH"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du demande de congé."
      "responses": 
        "200": 
          "description": "Circuit : étapes, étape courante, responsable, échéances, BLOCAGES"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/leave_request/{id}/available-actions": 
    "get": 
      "operationId": "get_leave_request_available_actions"
      "summary": "Ce que l'agent peut faire MAINTENANT sur cet objet, et pourquoi pas le reste"
      "description": "Ce que l'agent peut faire MAINTENANT sur cet objet, et pourquoi pas le reste"
      "tags": 
        - "RH"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du demande de congé."
      "responses": 
        "200": 
          "description": "Ce que l'agent peut faire MAINTENANT sur cet objet, et pourquoi pas le reste"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/admin_request": 
    "get": 
      "operationId": "list_admin_request"
      "summary": "Lister : Demande au bureau du secrétariat"
      "description": |-
          Demande d'achat, de mission, de course ou de prestation traitée par l'assistante de direction, avec son circuit de validation et son imputation budgétaire à la clôture.
          
          Filtres possibles sur : id, reference, type, subtype, title, status, priority, requesterId, assignedToId, validatorId, departmentId, deadline, companyId, createdAt, description. Syntaxe : `champ=valeur`, `champ=in:A,B`, `champ=gte:2026-01-01`, `champ=contains:texte`, `champ=null`.
      "tags": 
        - "ADMIN_REQUESTS"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "q"
          "in": "query"
          "schema": 
            "type": "string"
          "description": "Recherche plein texte sur : reference, title, description."
        -           "name": "sort"
          "in": "query"
          "schema": 
            "type": "string"
          "description": "Tri « champ:asc » ou « champ:desc »."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Liste paginée de demande au bureau du secrétariat."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/ListResult"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/admin_request/{id}": 
    "get": 
      "operationId": "get_admin_request"
      "summary": "Lire un demande au bureau du secrétariat"
      "description": "Demande d'achat, de mission, de course ou de prestation traitée par l'assistante de direction, avec son circuit de validation et son imputation budgétaire à la clôture. Rend la fiche complète et les liens vers ses facettes (historique, pièces, circuit, actions)."
      "tags": 
        - "ADMIN_REQUESTS"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du demande au bureau du secrétariat."
      "responses": 
        "200": 
          "description": "Fiche complète."
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/admin_request/{id}/history": 
    "get": 
      "operationId": "get_admin_request_history"
      "summary": "Historique — actions humaines ET appels d'agents, distingués"
      "description": "Historique — actions humaines ET appels d'agents, distingués"
      "tags": 
        - "ADMIN_REQUESTS"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du demande au bureau du secrétariat."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Historique — actions humaines ET appels d'agents, distingués"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/admin_request/{id}/documents": 
    "get": 
      "operationId": "list_admin_request_documents"
      "summary": "Pièces jointes, avec leur chemin de téléchargement contrôlé"
      "description": "Pièces jointes, avec leur chemin de téléchargement contrôlé"
      "tags": 
        - "ADMIN_REQUESTS"
      "security": 
        -           "apiKey": 
            - "erp.documents.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du demande au bureau du secrétariat."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Pièces jointes, avec leur chemin de téléchargement contrôlé"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/admin_request/{id}/comments": 
    "get": 
      "operationId": "list_admin_request_comments"
      "summary": "Commentaires portés sur l'objet"
      "description": "Commentaires portés sur l'objet"
      "tags": 
        - "ADMIN_REQUESTS"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du demande au bureau du secrétariat."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Commentaires portés sur l'objet"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/admin_request/{id}/related": 
    "get": 
      "operationId": "get_admin_request_related"
      "summary": "Objets liés (sous-objets, lignes, participants)"
      "description": "Objets liés (sous-objets, lignes, participants)"
      "tags": 
        - "ADMIN_REQUESTS"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du demande au bureau du secrétariat."
      "responses": 
        "200": 
          "description": "Objets liés (sous-objets, lignes, participants)"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/admin_request/{id}/workflow": 
    "get": 
      "operationId": "get_admin_request_workflow"
      "summary": "Circuit : étapes, étape courante, responsable, échéances, BLOCAGES"
      "description": "Représentation structurée du circuit : étapes du workflow, celles déjà réalisées, l'étape courante, les prochaines possibles, le responsable, les dates et échéances, les pièces manquantes et les conditions à réunir pour avancer. C'est la réponse à « qu'est-ce qui bloque ce dossier ? »."
      "tags": 
        - "ADMIN_REQUESTS"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du demande au bureau du secrétariat."
      "responses": 
        "200": 
          "description": "Circuit : étapes, étape courante, responsable, échéances, BLOCAGES"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/admin_request/{id}/available-actions": 
    "get": 
      "operationId": "get_admin_request_available_actions"
      "summary": "Ce que l'agent peut faire MAINTENANT sur cet objet, et pourquoi pas le reste"
      "description": "Ce que l'agent peut faire MAINTENANT sur cet objet, et pourquoi pas le reste"
      "tags": 
        - "ADMIN_REQUESTS"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du demande au bureau du secrétariat."
      "responses": 
        "200": 
          "description": "Ce que l'agent peut faire MAINTENANT sur cet objet, et pourquoi pas le reste"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/sponsoring": 
    "get": 
      "operationId": "list_sponsoring"
      "summary": "Lister : Demande de sponsoring"
      "description": |-
          Demande de parrainage instruite par le circuit Ad & Pro (National Sales → chef de produit → Direction), avec ses postes de dépense et son ordre de paiement.
          
          Filtres possibles sur : id, reference, institution, doctor, type, status, amountRequested, amountGranted, companyId, requestDate, createdAt, description, comments. Syntaxe : `champ=valeur`, `champ=in:A,B`, `champ=gte:2026-01-01`, `champ=contains:texte`, `champ=null`.
      "tags": 
        - "SPONSORING"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "q"
          "in": "query"
          "schema": 
            "type": "string"
          "description": "Recherche plein texte sur : reference, institution, doctor, description, comments."
        -           "name": "sort"
          "in": "query"
          "schema": 
            "type": "string"
          "description": "Tri « champ:asc » ou « champ:desc »."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Liste paginée de demande de sponsoring."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/ListResult"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/sponsoring/{id}": 
    "get": 
      "operationId": "get_sponsoring"
      "summary": "Lire un demande de sponsoring"
      "description": "Demande de parrainage instruite par le circuit Ad & Pro (National Sales → chef de produit → Direction), avec ses postes de dépense et son ordre de paiement. Rend la fiche complète et les liens vers ses facettes (historique, pièces, circuit, actions)."
      "tags": 
        - "SPONSORING"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du demande de sponsoring."
      "responses": 
        "200": 
          "description": "Fiche complète."
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/sponsoring/{id}/history": 
    "get": 
      "operationId": "get_sponsoring_history"
      "summary": "Historique — actions humaines ET appels d'agents, distingués"
      "description": "Historique — actions humaines ET appels d'agents, distingués"
      "tags": 
        - "SPONSORING"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du demande de sponsoring."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Historique — actions humaines ET appels d'agents, distingués"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/sponsoring/{id}/documents": 
    "get": 
      "operationId": "list_sponsoring_documents"
      "summary": "Pièces jointes, avec leur chemin de téléchargement contrôlé"
      "description": "Pièces jointes, avec leur chemin de téléchargement contrôlé"
      "tags": 
        - "SPONSORING"
      "security": 
        -           "apiKey": 
            - "erp.documents.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du demande de sponsoring."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Pièces jointes, avec leur chemin de téléchargement contrôlé"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/sponsoring/{id}/comments": 
    "get": 
      "operationId": "list_sponsoring_comments"
      "summary": "Commentaires portés sur l'objet"
      "description": "Commentaires portés sur l'objet"
      "tags": 
        - "SPONSORING"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du demande de sponsoring."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Commentaires portés sur l'objet"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/sponsoring/{id}/related": 
    "get": 
      "operationId": "get_sponsoring_related"
      "summary": "Objets liés (sous-objets, lignes, participants)"
      "description": "Objets liés (sous-objets, lignes, participants)"
      "tags": 
        - "SPONSORING"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du demande de sponsoring."
      "responses": 
        "200": 
          "description": "Objets liés (sous-objets, lignes, participants)"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/sponsoring/{id}/workflow": 
    "get": 
      "operationId": "get_sponsoring_workflow"
      "summary": "Circuit : étapes, étape courante, responsable, échéances, BLOCAGES"
      "description": "Représentation structurée du circuit : étapes du workflow, celles déjà réalisées, l'étape courante, les prochaines possibles, le responsable, les dates et échéances, les pièces manquantes et les conditions à réunir pour avancer. C'est la réponse à « qu'est-ce qui bloque ce dossier ? »."
      "tags": 
        - "SPONSORING"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du demande de sponsoring."
      "responses": 
        "200": 
          "description": "Circuit : étapes, étape courante, responsable, échéances, BLOCAGES"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/sponsoring/{id}/available-actions": 
    "get": 
      "operationId": "get_sponsoring_available_actions"
      "summary": "Ce que l'agent peut faire MAINTENANT sur cet objet, et pourquoi pas le reste"
      "description": "Ce que l'agent peut faire MAINTENANT sur cet objet, et pourquoi pas le reste"
      "tags": 
        - "SPONSORING"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du demande de sponsoring."
      "responses": 
        "200": 
          "description": "Ce que l'agent peut faire MAINTENANT sur cet objet, et pourquoi pas le reste"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/congress_international": 
    "get": 
      "operationId": "list_congress_international"
      "summary": "Lister : Congrès international"
      "description": |-
          Participation à un congrès à l'étranger : circuit de validation, inscrits, postes de dépense.
          
          Filtres possibles sur : id, name, country, city, startDate, endDate, status, requestStatus, estimatedBudget, companyId, specialty. Syntaxe : `champ=valeur`, `champ=in:A,B`, `champ=gte:2026-01-01`, `champ=contains:texte`, `champ=null`.
      "tags": 
        - "CONGRESS_INTERNATIONAL"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "q"
          "in": "query"
          "schema": 
            "type": "string"
          "description": "Recherche plein texte sur : name, country, city, specialty."
        -           "name": "sort"
          "in": "query"
          "schema": 
            "type": "string"
          "description": "Tri « champ:asc » ou « champ:desc »."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Liste paginée de congrès international."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/ListResult"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/congress_international/{id}": 
    "get": 
      "operationId": "get_congress_international"
      "summary": "Lire un congrès international"
      "description": "Participation à un congrès à l'étranger : circuit de validation, inscrits, postes de dépense. Rend la fiche complète et les liens vers ses facettes (historique, pièces, circuit, actions)."
      "tags": 
        - "CONGRESS_INTERNATIONAL"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du congrès international."
      "responses": 
        "200": 
          "description": "Fiche complète."
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/congress_international/{id}/history": 
    "get": 
      "operationId": "get_congress_international_history"
      "summary": "Historique — actions humaines ET appels d'agents, distingués"
      "description": "Historique — actions humaines ET appels d'agents, distingués"
      "tags": 
        - "CONGRESS_INTERNATIONAL"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du congrès international."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Historique — actions humaines ET appels d'agents, distingués"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/congress_international/{id}/documents": 
    "get": 
      "operationId": "list_congress_international_documents"
      "summary": "Pièces jointes, avec leur chemin de téléchargement contrôlé"
      "description": "Pièces jointes, avec leur chemin de téléchargement contrôlé"
      "tags": 
        - "CONGRESS_INTERNATIONAL"
      "security": 
        -           "apiKey": 
            - "erp.documents.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du congrès international."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Pièces jointes, avec leur chemin de téléchargement contrôlé"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/congress_international/{id}/comments": 
    "get": 
      "operationId": "list_congress_international_comments"
      "summary": "Commentaires portés sur l'objet"
      "description": "Commentaires portés sur l'objet"
      "tags": 
        - "CONGRESS_INTERNATIONAL"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du congrès international."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Commentaires portés sur l'objet"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/congress_international/{id}/related": 
    "get": 
      "operationId": "get_congress_international_related"
      "summary": "Objets liés (sous-objets, lignes, participants)"
      "description": "Objets liés (sous-objets, lignes, participants)"
      "tags": 
        - "CONGRESS_INTERNATIONAL"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du congrès international."
      "responses": 
        "200": 
          "description": "Objets liés (sous-objets, lignes, participants)"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/congress_international/{id}/workflow": 
    "get": 
      "operationId": "get_congress_international_workflow"
      "summary": "Circuit : étapes, étape courante, responsable, échéances, BLOCAGES"
      "description": "Représentation structurée du circuit : étapes du workflow, celles déjà réalisées, l'étape courante, les prochaines possibles, le responsable, les dates et échéances, les pièces manquantes et les conditions à réunir pour avancer. C'est la réponse à « qu'est-ce qui bloque ce dossier ? »."
      "tags": 
        - "CONGRESS_INTERNATIONAL"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du congrès international."
      "responses": 
        "200": 
          "description": "Circuit : étapes, étape courante, responsable, échéances, BLOCAGES"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/congress_international/{id}/available-actions": 
    "get": 
      "operationId": "get_congress_international_available_actions"
      "summary": "Ce que l'agent peut faire MAINTENANT sur cet objet, et pourquoi pas le reste"
      "description": "Ce que l'agent peut faire MAINTENANT sur cet objet, et pourquoi pas le reste"
      "tags": 
        - "CONGRESS_INTERNATIONAL"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du congrès international."
      "responses": 
        "200": 
          "description": "Ce que l'agent peut faire MAINTENANT sur cet objet, et pourquoi pas le reste"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/congress_national": 
    "get": 
      "operationId": "list_congress_national"
      "summary": "Lister : Congrès national"
      "description": |-
          Participation à un congrès en Algérie, même circuit que l'international.
          
          Filtres possibles sur : id, name, city, hostInstitution, date, status, requestStatus, estimatedBudget, companyId, specialty. Syntaxe : `champ=valeur`, `champ=in:A,B`, `champ=gte:2026-01-01`, `champ=contains:texte`, `champ=null`.
      "tags": 
        - "CONGRESS_NATIONAL"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "q"
          "in": "query"
          "schema": 
            "type": "string"
          "description": "Recherche plein texte sur : name, city, hostInstitution, specialty."
        -           "name": "sort"
          "in": "query"
          "schema": 
            "type": "string"
          "description": "Tri « champ:asc » ou « champ:desc »."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Liste paginée de congrès national."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/ListResult"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/congress_national/{id}": 
    "get": 
      "operationId": "get_congress_national"
      "summary": "Lire un congrès national"
      "description": "Participation à un congrès en Algérie, même circuit que l'international. Rend la fiche complète et les liens vers ses facettes (historique, pièces, circuit, actions)."
      "tags": 
        - "CONGRESS_NATIONAL"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du congrès national."
      "responses": 
        "200": 
          "description": "Fiche complète."
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/congress_national/{id}/history": 
    "get": 
      "operationId": "get_congress_national_history"
      "summary": "Historique — actions humaines ET appels d'agents, distingués"
      "description": "Historique — actions humaines ET appels d'agents, distingués"
      "tags": 
        - "CONGRESS_NATIONAL"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du congrès national."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Historique — actions humaines ET appels d'agents, distingués"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/congress_national/{id}/documents": 
    "get": 
      "operationId": "list_congress_national_documents"
      "summary": "Pièces jointes, avec leur chemin de téléchargement contrôlé"
      "description": "Pièces jointes, avec leur chemin de téléchargement contrôlé"
      "tags": 
        - "CONGRESS_NATIONAL"
      "security": 
        -           "apiKey": 
            - "erp.documents.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du congrès national."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Pièces jointes, avec leur chemin de téléchargement contrôlé"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/congress_national/{id}/comments": 
    "get": 
      "operationId": "list_congress_national_comments"
      "summary": "Commentaires portés sur l'objet"
      "description": "Commentaires portés sur l'objet"
      "tags": 
        - "CONGRESS_NATIONAL"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du congrès national."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Commentaires portés sur l'objet"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/congress_national/{id}/related": 
    "get": 
      "operationId": "get_congress_national_related"
      "summary": "Objets liés (sous-objets, lignes, participants)"
      "description": "Objets liés (sous-objets, lignes, participants)"
      "tags": 
        - "CONGRESS_NATIONAL"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du congrès national."
      "responses": 
        "200": 
          "description": "Objets liés (sous-objets, lignes, participants)"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/congress_national/{id}/workflow": 
    "get": 
      "operationId": "get_congress_national_workflow"
      "summary": "Circuit : étapes, étape courante, responsable, échéances, BLOCAGES"
      "description": "Représentation structurée du circuit : étapes du workflow, celles déjà réalisées, l'étape courante, les prochaines possibles, le responsable, les dates et échéances, les pièces manquantes et les conditions à réunir pour avancer. C'est la réponse à « qu'est-ce qui bloque ce dossier ? »."
      "tags": 
        - "CONGRESS_NATIONAL"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du congrès national."
      "responses": 
        "200": 
          "description": "Circuit : étapes, étape courante, responsable, échéances, BLOCAGES"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/congress_national/{id}/available-actions": 
    "get": 
      "operationId": "get_congress_national_available_actions"
      "summary": "Ce que l'agent peut faire MAINTENANT sur cet objet, et pourquoi pas le reste"
      "description": "Ce que l'agent peut faire MAINTENANT sur cet objet, et pourquoi pas le reste"
      "tags": 
        - "CONGRESS_NATIONAL"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du congrès national."
      "responses": 
        "200": 
          "description": "Ce que l'agent peut faire MAINTENANT sur cet objet, et pourquoi pas le reste"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/event": 
    "get": 
      "operationId": "list_event"
      "summary": "Lister : Événement"
      "description": |-
          Événement organisé par l'entreprise (soirée scientifique, atelier…), avec son circuit de validation selon l'origine de la demande.
          
          Filtres possibles sur : id, name, type, scope, status, requestStatus, startDate, endDate, city, estimatedBudget, companyId, location, description. Syntaxe : `champ=valeur`, `champ=in:A,B`, `champ=gte:2026-01-01`, `champ=contains:texte`, `champ=null`.
      "tags": 
        - "EVENTS"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "q"
          "in": "query"
          "schema": 
            "type": "string"
          "description": "Recherche plein texte sur : name, location, city, description."
        -           "name": "sort"
          "in": "query"
          "schema": 
            "type": "string"
          "description": "Tri « champ:asc » ou « champ:desc »."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Liste paginée de événement."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/ListResult"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/event/{id}": 
    "get": 
      "operationId": "get_event"
      "summary": "Lire un événement"
      "description": "Événement organisé par l'entreprise (soirée scientifique, atelier…), avec son circuit de validation selon l'origine de la demande. Rend la fiche complète et les liens vers ses facettes (historique, pièces, circuit, actions)."
      "tags": 
        - "EVENTS"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du événement."
      "responses": 
        "200": 
          "description": "Fiche complète."
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/event/{id}/history": 
    "get": 
      "operationId": "get_event_history"
      "summary": "Historique — actions humaines ET appels d'agents, distingués"
      "description": "Historique — actions humaines ET appels d'agents, distingués"
      "tags": 
        - "EVENTS"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du événement."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Historique — actions humaines ET appels d'agents, distingués"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/event/{id}/documents": 
    "get": 
      "operationId": "list_event_documents"
      "summary": "Pièces jointes, avec leur chemin de téléchargement contrôlé"
      "description": "Pièces jointes, avec leur chemin de téléchargement contrôlé"
      "tags": 
        - "EVENTS"
      "security": 
        -           "apiKey": 
            - "erp.documents.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du événement."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Pièces jointes, avec leur chemin de téléchargement contrôlé"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/event/{id}/comments": 
    "get": 
      "operationId": "list_event_comments"
      "summary": "Commentaires portés sur l'objet"
      "description": "Commentaires portés sur l'objet"
      "tags": 
        - "EVENTS"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du événement."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Commentaires portés sur l'objet"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/event/{id}/related": 
    "get": 
      "operationId": "get_event_related"
      "summary": "Objets liés (sous-objets, lignes, participants)"
      "description": "Objets liés (sous-objets, lignes, participants)"
      "tags": 
        - "EVENTS"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du événement."
      "responses": 
        "200": 
          "description": "Objets liés (sous-objets, lignes, participants)"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/event/{id}/workflow": 
    "get": 
      "operationId": "get_event_workflow"
      "summary": "Circuit : étapes, étape courante, responsable, échéances, BLOCAGES"
      "description": "Représentation structurée du circuit : étapes du workflow, celles déjà réalisées, l'étape courante, les prochaines possibles, le responsable, les dates et échéances, les pièces manquantes et les conditions à réunir pour avancer. C'est la réponse à « qu'est-ce qui bloque ce dossier ? »."
      "tags": 
        - "EVENTS"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du événement."
      "responses": 
        "200": 
          "description": "Circuit : étapes, étape courante, responsable, échéances, BLOCAGES"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/event/{id}/available-actions": 
    "get": 
      "operationId": "get_event_available_actions"
      "summary": "Ce que l'agent peut faire MAINTENANT sur cet objet, et pourquoi pas le reste"
      "description": "Ce que l'agent peut faire MAINTENANT sur cet objet, et pourquoi pas le reste"
      "tags": 
        - "EVENTS"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du événement."
      "responses": 
        "200": 
          "description": "Ce que l'agent peut faire MAINTENANT sur cet objet, et pourquoi pas le reste"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/expense_order": 
    "get": 
      "operationId": "list_expense_order"
      "summary": "Lister : Ordre de dépense"
      "description": |-
          Engagement de paiement émis par un module métier (bon de virement Regulatory, poste Ad & Pro…) et réglé par les finances.
          
          Filtres possibles sur : id, reference, label, beneficiary, amount, category, status, dueDate, sourceType, sourceId, companyId, createdAt, notes. Syntaxe : `champ=valeur`, `champ=in:A,B`, `champ=gte:2026-01-01`, `champ=contains:texte`, `champ=null`.
      "tags": 
        - "FINANCES"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "q"
          "in": "query"
          "schema": 
            "type": "string"
          "description": "Recherche plein texte sur : reference, label, beneficiary, notes."
        -           "name": "sort"
          "in": "query"
          "schema": 
            "type": "string"
          "description": "Tri « champ:asc » ou « champ:desc »."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Liste paginée de ordre de dépense."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/ListResult"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/expense_order/{id}": 
    "get": 
      "operationId": "get_expense_order"
      "summary": "Lire un ordre de dépense"
      "description": "Engagement de paiement émis par un module métier (bon de virement Regulatory, poste Ad & Pro…) et réglé par les finances. Rend la fiche complète et les liens vers ses facettes (historique, pièces, circuit, actions)."
      "tags": 
        - "FINANCES"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du ordre de dépense."
      "responses": 
        "200": 
          "description": "Fiche complète."
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/expense_order/{id}/history": 
    "get": 
      "operationId": "get_expense_order_history"
      "summary": "Historique — actions humaines ET appels d'agents, distingués"
      "description": "Historique — actions humaines ET appels d'agents, distingués"
      "tags": 
        - "FINANCES"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du ordre de dépense."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Historique — actions humaines ET appels d'agents, distingués"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/expense_order/{id}/documents": 
    "get": 
      "operationId": "list_expense_order_documents"
      "summary": "Pièces jointes, avec leur chemin de téléchargement contrôlé"
      "description": "Pièces jointes, avec leur chemin de téléchargement contrôlé"
      "tags": 
        - "FINANCES"
      "security": 
        -           "apiKey": 
            - "erp.documents.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du ordre de dépense."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Pièces jointes, avec leur chemin de téléchargement contrôlé"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/expense_order/{id}/comments": 
    "get": 
      "operationId": "list_expense_order_comments"
      "summary": "Commentaires portés sur l'objet"
      "description": "Commentaires portés sur l'objet"
      "tags": 
        - "FINANCES"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du ordre de dépense."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Commentaires portés sur l'objet"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/expense_order/{id}/related": 
    "get": 
      "operationId": "get_expense_order_related"
      "summary": "Objets liés (sous-objets, lignes, participants)"
      "description": "Objets liés (sous-objets, lignes, participants)"
      "tags": 
        - "FINANCES"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du ordre de dépense."
      "responses": 
        "200": 
          "description": "Objets liés (sous-objets, lignes, participants)"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/expense_order/{id}/workflow": 
    "get": 
      "operationId": "get_expense_order_workflow"
      "summary": "Circuit : étapes, étape courante, responsable, échéances, BLOCAGES"
      "description": "Représentation structurée du circuit : étapes du workflow, celles déjà réalisées, l'étape courante, les prochaines possibles, le responsable, les dates et échéances, les pièces manquantes et les conditions à réunir pour avancer. C'est la réponse à « qu'est-ce qui bloque ce dossier ? »."
      "tags": 
        - "FINANCES"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du ordre de dépense."
      "responses": 
        "200": 
          "description": "Circuit : étapes, étape courante, responsable, échéances, BLOCAGES"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/expense_order/{id}/available-actions": 
    "get": 
      "operationId": "get_expense_order_available_actions"
      "summary": "Ce que l'agent peut faire MAINTENANT sur cet objet, et pourquoi pas le reste"
      "description": "Ce que l'agent peut faire MAINTENANT sur cet objet, et pourquoi pas le reste"
      "tags": 
        - "FINANCES"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du ordre de dépense."
      "responses": 
        "200": 
          "description": "Ce que l'agent peut faire MAINTENANT sur cet objet, et pourquoi pas le reste"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/department_budget": 
    "get": 
      "operationId": "list_department_budget"
      "summary": "Lister : Budget de département"
      "description": |-
          Enveloppe annuelle d'un département pour une nature donnée : moyens généraux, masse salariale, budget métier, formation.
          
          Filtres possibles sur : id, departmentId, year, kind, amount, updatedAt. Syntaxe : `champ=valeur`, `champ=in:A,B`, `champ=gte:2026-01-01`, `champ=contains:texte`, `champ=null`.
      "tags": 
        - "BUDGETS"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "q"
          "in": "query"
          "schema": 
            "type": "string"
          "description": "Recherche plein texte sur : —."
        -           "name": "sort"
          "in": "query"
          "schema": 
            "type": "string"
          "description": "Tri « champ:asc » ou « champ:desc »."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Liste paginée de budget de département."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/ListResult"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/department_budget/{id}": 
    "get": 
      "operationId": "get_department_budget"
      "summary": "Lire un budget de département"
      "description": "Enveloppe annuelle d'un département pour une nature donnée : moyens généraux, masse salariale, budget métier, formation. Rend la fiche complète et les liens vers ses facettes (historique, pièces, circuit, actions)."
      "tags": 
        - "BUDGETS"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du budget de département."
      "responses": 
        "200": 
          "description": "Fiche complète."
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/department_budget/{id}/history": 
    "get": 
      "operationId": "get_department_budget_history"
      "summary": "Historique — actions humaines ET appels d'agents, distingués"
      "description": "Historique — actions humaines ET appels d'agents, distingués"
      "tags": 
        - "BUDGETS"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du budget de département."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Historique — actions humaines ET appels d'agents, distingués"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/department_budget/{id}/documents": 
    "get": 
      "operationId": "list_department_budget_documents"
      "summary": "Pièces jointes, avec leur chemin de téléchargement contrôlé"
      "description": "Pièces jointes, avec leur chemin de téléchargement contrôlé"
      "tags": 
        - "BUDGETS"
      "security": 
        -           "apiKey": 
            - "erp.documents.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du budget de département."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Pièces jointes, avec leur chemin de téléchargement contrôlé"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/department_budget/{id}/comments": 
    "get": 
      "operationId": "list_department_budget_comments"
      "summary": "Commentaires portés sur l'objet"
      "description": "Commentaires portés sur l'objet"
      "tags": 
        - "BUDGETS"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du budget de département."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Commentaires portés sur l'objet"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/department_budget/{id}/related": 
    "get": 
      "operationId": "get_department_budget_related"
      "summary": "Objets liés (sous-objets, lignes, participants)"
      "description": "Objets liés (sous-objets, lignes, participants)"
      "tags": 
        - "BUDGETS"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du budget de département."
      "responses": 
        "200": 
          "description": "Objets liés (sous-objets, lignes, participants)"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/department_budget/{id}/workflow": 
    "get": 
      "operationId": "get_department_budget_workflow"
      "summary": "Circuit : étapes, étape courante, responsable, échéances, BLOCAGES"
      "description": "Représentation structurée du circuit : étapes du workflow, celles déjà réalisées, l'étape courante, les prochaines possibles, le responsable, les dates et échéances, les pièces manquantes et les conditions à réunir pour avancer. C'est la réponse à « qu'est-ce qui bloque ce dossier ? »."
      "tags": 
        - "BUDGETS"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du budget de département."
      "responses": 
        "200": 
          "description": "Circuit : étapes, étape courante, responsable, échéances, BLOCAGES"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/department_budget/{id}/available-actions": 
    "get": 
      "operationId": "get_department_budget_available_actions"
      "summary": "Ce que l'agent peut faire MAINTENANT sur cet objet, et pourquoi pas le reste"
      "description": "Ce que l'agent peut faire MAINTENANT sur cet objet, et pourquoi pas le reste"
      "tags": 
        - "BUDGETS"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du budget de département."
      "responses": 
        "200": 
          "description": "Ce que l'agent peut faire MAINTENANT sur cet objet, et pourquoi pas le reste"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/department_expense": 
    "get": 
      "operationId": "list_department_expense"
      "summary": "Lister : Dépense imputée"
      "description": |-
          Achat imputé à un budget départemental, avec son justificatif obligatoire et le détail des articles du ticket.
          
          Filtres possibles sur : id, departmentId, year, kind, label, amount, date, pettyCashId, adminRequestId, notes. Syntaxe : `champ=valeur`, `champ=in:A,B`, `champ=gte:2026-01-01`, `champ=contains:texte`, `champ=null`.
      "tags": 
        - "BUDGETS"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "q"
          "in": "query"
          "schema": 
            "type": "string"
          "description": "Recherche plein texte sur : label, notes."
        -           "name": "sort"
          "in": "query"
          "schema": 
            "type": "string"
          "description": "Tri « champ:asc » ou « champ:desc »."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Liste paginée de dépense imputée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/ListResult"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/department_expense/{id}": 
    "get": 
      "operationId": "get_department_expense"
      "summary": "Lire un dépense imputée"
      "description": "Achat imputé à un budget départemental, avec son justificatif obligatoire et le détail des articles du ticket. Rend la fiche complète et les liens vers ses facettes (historique, pièces, circuit, actions)."
      "tags": 
        - "BUDGETS"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du dépense imputée."
      "responses": 
        "200": 
          "description": "Fiche complète."
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/department_expense/{id}/history": 
    "get": 
      "operationId": "get_department_expense_history"
      "summary": "Historique — actions humaines ET appels d'agents, distingués"
      "description": "Historique — actions humaines ET appels d'agents, distingués"
      "tags": 
        - "BUDGETS"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du dépense imputée."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Historique — actions humaines ET appels d'agents, distingués"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/department_expense/{id}/documents": 
    "get": 
      "operationId": "list_department_expense_documents"
      "summary": "Pièces jointes, avec leur chemin de téléchargement contrôlé"
      "description": "Pièces jointes, avec leur chemin de téléchargement contrôlé"
      "tags": 
        - "BUDGETS"
      "security": 
        -           "apiKey": 
            - "erp.documents.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du dépense imputée."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Pièces jointes, avec leur chemin de téléchargement contrôlé"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/department_expense/{id}/comments": 
    "get": 
      "operationId": "list_department_expense_comments"
      "summary": "Commentaires portés sur l'objet"
      "description": "Commentaires portés sur l'objet"
      "tags": 
        - "BUDGETS"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du dépense imputée."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Commentaires portés sur l'objet"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/department_expense/{id}/related": 
    "get": 
      "operationId": "get_department_expense_related"
      "summary": "Objets liés (sous-objets, lignes, participants)"
      "description": "Objets liés (sous-objets, lignes, participants)"
      "tags": 
        - "BUDGETS"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du dépense imputée."
      "responses": 
        "200": 
          "description": "Objets liés (sous-objets, lignes, participants)"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/department_expense/{id}/workflow": 
    "get": 
      "operationId": "get_department_expense_workflow"
      "summary": "Circuit : étapes, étape courante, responsable, échéances, BLOCAGES"
      "description": "Représentation structurée du circuit : étapes du workflow, celles déjà réalisées, l'étape courante, les prochaines possibles, le responsable, les dates et échéances, les pièces manquantes et les conditions à réunir pour avancer. C'est la réponse à « qu'est-ce qui bloque ce dossier ? »."
      "tags": 
        - "BUDGETS"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du dépense imputée."
      "responses": 
        "200": 
          "description": "Circuit : étapes, étape courante, responsable, échéances, BLOCAGES"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/department_expense/{id}/available-actions": 
    "get": 
      "operationId": "get_department_expense_available_actions"
      "summary": "Ce que l'agent peut faire MAINTENANT sur cet objet, et pourquoi pas le reste"
      "description": "Ce que l'agent peut faire MAINTENANT sur cet objet, et pourquoi pas le reste"
      "tags": 
        - "BUDGETS"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du dépense imputée."
      "responses": 
        "200": 
          "description": "Ce que l'agent peut faire MAINTENANT sur cet objet, et pourquoi pas le reste"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/stock_snapshot": 
    "get": 
      "operationId": "list_stock_snapshot"
      "summary": "Lister : État de stock daté"
      "description": |-
          Relevé « à cette date, il reste X » pour un produit sur un lieu (PCH, hôpital, annexe).
          
          Filtres possibles sur : id, productId, date, quantity, scope, annexId, companyId. Syntaxe : `champ=valeur`, `champ=in:A,B`, `champ=gte:2026-01-01`, `champ=contains:texte`, `champ=null`.
      "tags": 
        - "STOCKS"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "q"
          "in": "query"
          "schema": 
            "type": "string"
          "description": "Recherche plein texte sur : —."
        -           "name": "sort"
          "in": "query"
          "schema": 
            "type": "string"
          "description": "Tri « champ:asc » ou « champ:desc »."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Liste paginée de état de stock daté."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/ListResult"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/stock_snapshot/{id}": 
    "get": 
      "operationId": "get_stock_snapshot"
      "summary": "Lire un état de stock daté"
      "description": "Relevé « à cette date, il reste X » pour un produit sur un lieu (PCH, hôpital, annexe). Rend la fiche complète et les liens vers ses facettes (historique, pièces, circuit, actions)."
      "tags": 
        - "STOCKS"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du état de stock daté."
      "responses": 
        "200": 
          "description": "Fiche complète."
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/stock_snapshot/{id}/history": 
    "get": 
      "operationId": "get_stock_snapshot_history"
      "summary": "Historique — actions humaines ET appels d'agents, distingués"
      "description": "Historique — actions humaines ET appels d'agents, distingués"
      "tags": 
        - "STOCKS"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du état de stock daté."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Historique — actions humaines ET appels d'agents, distingués"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/stock_snapshot/{id}/documents": 
    "get": 
      "operationId": "list_stock_snapshot_documents"
      "summary": "Pièces jointes, avec leur chemin de téléchargement contrôlé"
      "description": "Pièces jointes, avec leur chemin de téléchargement contrôlé"
      "tags": 
        - "STOCKS"
      "security": 
        -           "apiKey": 
            - "erp.documents.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du état de stock daté."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Pièces jointes, avec leur chemin de téléchargement contrôlé"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/stock_snapshot/{id}/comments": 
    "get": 
      "operationId": "list_stock_snapshot_comments"
      "summary": "Commentaires portés sur l'objet"
      "description": "Commentaires portés sur l'objet"
      "tags": 
        - "STOCKS"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du état de stock daté."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Commentaires portés sur l'objet"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/stock_snapshot/{id}/related": 
    "get": 
      "operationId": "get_stock_snapshot_related"
      "summary": "Objets liés (sous-objets, lignes, participants)"
      "description": "Objets liés (sous-objets, lignes, participants)"
      "tags": 
        - "STOCKS"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du état de stock daté."
      "responses": 
        "200": 
          "description": "Objets liés (sous-objets, lignes, participants)"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/stock_snapshot/{id}/workflow": 
    "get": 
      "operationId": "get_stock_snapshot_workflow"
      "summary": "Circuit : étapes, étape courante, responsable, échéances, BLOCAGES"
      "description": "Représentation structurée du circuit : étapes du workflow, celles déjà réalisées, l'étape courante, les prochaines possibles, le responsable, les dates et échéances, les pièces manquantes et les conditions à réunir pour avancer. C'est la réponse à « qu'est-ce qui bloque ce dossier ? »."
      "tags": 
        - "STOCKS"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du état de stock daté."
      "responses": 
        "200": 
          "description": "Circuit : étapes, étape courante, responsable, échéances, BLOCAGES"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/stock_snapshot/{id}/available-actions": 
    "get": 
      "operationId": "get_stock_snapshot_available_actions"
      "summary": "Ce que l'agent peut faire MAINTENANT sur cet objet, et pourquoi pas le reste"
      "description": "Ce que l'agent peut faire MAINTENANT sur cet objet, et pourquoi pas le reste"
      "tags": 
        - "STOCKS"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du état de stock daté."
      "responses": 
        "200": 
          "description": "Ce que l'agent peut faire MAINTENANT sur cet objet, et pourquoi pas le reste"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/pch_tender": 
    "get": 
      "operationId": "list_pch_tender"
      "summary": "Lister : Appel d'offres PCH"
      "description": |-
          Appel d'offres de la Pharmacie Centrale des Hôpitaux, ses lignes de produits et l'analyse de marché associée.
          
          Filtres possibles sur : id, reference, title, client, supplier, value, awardDate, status, companyId, notes. Syntaxe : `champ=valeur`, `champ=in:A,B`, `champ=gte:2026-01-01`, `champ=contains:texte`, `champ=null`.
      "tags": 
        - "PCH"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "q"
          "in": "query"
          "schema": 
            "type": "string"
          "description": "Recherche plein texte sur : reference, title, client, supplier, notes."
        -           "name": "sort"
          "in": "query"
          "schema": 
            "type": "string"
          "description": "Tri « champ:asc » ou « champ:desc »."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Liste paginée de appel d'offres pch."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/ListResult"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/pch_tender/{id}": 
    "get": 
      "operationId": "get_pch_tender"
      "summary": "Lire un appel d'offres pch"
      "description": "Appel d'offres de la Pharmacie Centrale des Hôpitaux, ses lignes de produits et l'analyse de marché associée. Rend la fiche complète et les liens vers ses facettes (historique, pièces, circuit, actions)."
      "tags": 
        - "PCH"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du appel d'offres pch."
      "responses": 
        "200": 
          "description": "Fiche complète."
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/pch_tender/{id}/history": 
    "get": 
      "operationId": "get_pch_tender_history"
      "summary": "Historique — actions humaines ET appels d'agents, distingués"
      "description": "Historique — actions humaines ET appels d'agents, distingués"
      "tags": 
        - "PCH"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du appel d'offres pch."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Historique — actions humaines ET appels d'agents, distingués"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/pch_tender/{id}/documents": 
    "get": 
      "operationId": "list_pch_tender_documents"
      "summary": "Pièces jointes, avec leur chemin de téléchargement contrôlé"
      "description": "Pièces jointes, avec leur chemin de téléchargement contrôlé"
      "tags": 
        - "PCH"
      "security": 
        -           "apiKey": 
            - "erp.documents.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du appel d'offres pch."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Pièces jointes, avec leur chemin de téléchargement contrôlé"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/pch_tender/{id}/comments": 
    "get": 
      "operationId": "list_pch_tender_comments"
      "summary": "Commentaires portés sur l'objet"
      "description": "Commentaires portés sur l'objet"
      "tags": 
        - "PCH"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du appel d'offres pch."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Commentaires portés sur l'objet"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/pch_tender/{id}/related": 
    "get": 
      "operationId": "get_pch_tender_related"
      "summary": "Objets liés (sous-objets, lignes, participants)"
      "description": "Objets liés (sous-objets, lignes, participants)"
      "tags": 
        - "PCH"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du appel d'offres pch."
      "responses": 
        "200": 
          "description": "Objets liés (sous-objets, lignes, participants)"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/pch_tender/{id}/workflow": 
    "get": 
      "operationId": "get_pch_tender_workflow"
      "summary": "Circuit : étapes, étape courante, responsable, échéances, BLOCAGES"
      "description": "Représentation structurée du circuit : étapes du workflow, celles déjà réalisées, l'étape courante, les prochaines possibles, le responsable, les dates et échéances, les pièces manquantes et les conditions à réunir pour avancer. C'est la réponse à « qu'est-ce qui bloque ce dossier ? »."
      "tags": 
        - "PCH"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du appel d'offres pch."
      "responses": 
        "200": 
          "description": "Circuit : étapes, étape courante, responsable, échéances, BLOCAGES"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/pch_tender/{id}/available-actions": 
    "get": 
      "operationId": "get_pch_tender_available_actions"
      "summary": "Ce que l'agent peut faire MAINTENANT sur cet objet, et pourquoi pas le reste"
      "description": "Ce que l'agent peut faire MAINTENANT sur cet objet, et pourquoi pas le reste"
      "tags": 
        - "PCH"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du appel d'offres pch."
      "responses": 
        "200": 
          "description": "Ce que l'agent peut faire MAINTENANT sur cet objet, et pourquoi pas le reste"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/doctor": 
    "get": 
      "operationId": "list_doctor"
      "summary": "Lister : Médecin"
      "description": |-
          Praticien du fichier médical, visité par les délégués.
          
          Filtres possibles sur : id, name, title, specialty, sector, institution, city, region, delegateId, companyId, comments. Syntaxe : `champ=valeur`, `champ=in:A,B`, `champ=gte:2026-01-01`, `champ=contains:texte`, `champ=null`.
      "tags": 
        - "MEDICAL"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "q"
          "in": "query"
          "schema": 
            "type": "string"
          "description": "Recherche plein texte sur : name, specialty, institution, city, comments."
        -           "name": "sort"
          "in": "query"
          "schema": 
            "type": "string"
          "description": "Tri « champ:asc » ou « champ:desc »."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Liste paginée de médecin."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/ListResult"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/doctor/{id}": 
    "get": 
      "operationId": "get_doctor"
      "summary": "Lire un médecin"
      "description": "Praticien du fichier médical, visité par les délégués. Rend la fiche complète et les liens vers ses facettes (historique, pièces, circuit, actions)."
      "tags": 
        - "MEDICAL"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du médecin."
      "responses": 
        "200": 
          "description": "Fiche complète."
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/doctor/{id}/history": 
    "get": 
      "operationId": "get_doctor_history"
      "summary": "Historique — actions humaines ET appels d'agents, distingués"
      "description": "Historique — actions humaines ET appels d'agents, distingués"
      "tags": 
        - "MEDICAL"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du médecin."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Historique — actions humaines ET appels d'agents, distingués"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/doctor/{id}/documents": 
    "get": 
      "operationId": "list_doctor_documents"
      "summary": "Pièces jointes, avec leur chemin de téléchargement contrôlé"
      "description": "Pièces jointes, avec leur chemin de téléchargement contrôlé"
      "tags": 
        - "MEDICAL"
      "security": 
        -           "apiKey": 
            - "erp.documents.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du médecin."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Pièces jointes, avec leur chemin de téléchargement contrôlé"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/doctor/{id}/comments": 
    "get": 
      "operationId": "list_doctor_comments"
      "summary": "Commentaires portés sur l'objet"
      "description": "Commentaires portés sur l'objet"
      "tags": 
        - "MEDICAL"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du médecin."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Commentaires portés sur l'objet"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/doctor/{id}/related": 
    "get": 
      "operationId": "get_doctor_related"
      "summary": "Objets liés (sous-objets, lignes, participants)"
      "description": "Objets liés (sous-objets, lignes, participants)"
      "tags": 
        - "MEDICAL"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du médecin."
      "responses": 
        "200": 
          "description": "Objets liés (sous-objets, lignes, participants)"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/doctor/{id}/workflow": 
    "get": 
      "operationId": "get_doctor_workflow"
      "summary": "Circuit : étapes, étape courante, responsable, échéances, BLOCAGES"
      "description": "Représentation structurée du circuit : étapes du workflow, celles déjà réalisées, l'étape courante, les prochaines possibles, le responsable, les dates et échéances, les pièces manquantes et les conditions à réunir pour avancer. C'est la réponse à « qu'est-ce qui bloque ce dossier ? »."
      "tags": 
        - "MEDICAL"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du médecin."
      "responses": 
        "200": 
          "description": "Circuit : étapes, étape courante, responsable, échéances, BLOCAGES"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/doctor/{id}/available-actions": 
    "get": 
      "operationId": "get_doctor_available_actions"
      "summary": "Ce que l'agent peut faire MAINTENANT sur cet objet, et pourquoi pas le reste"
      "description": "Ce que l'agent peut faire MAINTENANT sur cet objet, et pourquoi pas le reste"
      "tags": 
        - "MEDICAL"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du médecin."
      "responses": 
        "200": 
          "description": "Ce que l'agent peut faire MAINTENANT sur cet objet, et pourquoi pas le reste"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/medical_visit": 
    "get": 
      "operationId": "list_medical_visit"
      "summary": "Lister : Visite médicale"
      "description": |-
          Visite d'un délégué chez un praticien, avec son compte rendu.
          
          Filtres possibles sur : id, doctorId, delegateId, date, status, objective, region, createdAt, report, doctorFeedback. Syntaxe : `champ=valeur`, `champ=in:A,B`, `champ=gte:2026-01-01`, `champ=contains:texte`, `champ=null`.
      "tags": 
        - "MEDICAL"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "q"
          "in": "query"
          "schema": 
            "type": "string"
          "description": "Recherche plein texte sur : report, objective, doctorFeedback."
        -           "name": "sort"
          "in": "query"
          "schema": 
            "type": "string"
          "description": "Tri « champ:asc » ou « champ:desc »."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Liste paginée de visite médicale."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/ListResult"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/medical_visit/{id}": 
    "get": 
      "operationId": "get_medical_visit"
      "summary": "Lire un visite médicale"
      "description": "Visite d'un délégué chez un praticien, avec son compte rendu. Rend la fiche complète et les liens vers ses facettes (historique, pièces, circuit, actions)."
      "tags": 
        - "MEDICAL"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du visite médicale."
      "responses": 
        "200": 
          "description": "Fiche complète."
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/medical_visit/{id}/history": 
    "get": 
      "operationId": "get_medical_visit_history"
      "summary": "Historique — actions humaines ET appels d'agents, distingués"
      "description": "Historique — actions humaines ET appels d'agents, distingués"
      "tags": 
        - "MEDICAL"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du visite médicale."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Historique — actions humaines ET appels d'agents, distingués"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/medical_visit/{id}/documents": 
    "get": 
      "operationId": "list_medical_visit_documents"
      "summary": "Pièces jointes, avec leur chemin de téléchargement contrôlé"
      "description": "Pièces jointes, avec leur chemin de téléchargement contrôlé"
      "tags": 
        - "MEDICAL"
      "security": 
        -           "apiKey": 
            - "erp.documents.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du visite médicale."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Pièces jointes, avec leur chemin de téléchargement contrôlé"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/medical_visit/{id}/comments": 
    "get": 
      "operationId": "list_medical_visit_comments"
      "summary": "Commentaires portés sur l'objet"
      "description": "Commentaires portés sur l'objet"
      "tags": 
        - "MEDICAL"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du visite médicale."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Commentaires portés sur l'objet"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/medical_visit/{id}/related": 
    "get": 
      "operationId": "get_medical_visit_related"
      "summary": "Objets liés (sous-objets, lignes, participants)"
      "description": "Objets liés (sous-objets, lignes, participants)"
      "tags": 
        - "MEDICAL"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du visite médicale."
      "responses": 
        "200": 
          "description": "Objets liés (sous-objets, lignes, participants)"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/medical_visit/{id}/workflow": 
    "get": 
      "operationId": "get_medical_visit_workflow"
      "summary": "Circuit : étapes, étape courante, responsable, échéances, BLOCAGES"
      "description": "Représentation structurée du circuit : étapes du workflow, celles déjà réalisées, l'étape courante, les prochaines possibles, le responsable, les dates et échéances, les pièces manquantes et les conditions à réunir pour avancer. C'est la réponse à « qu'est-ce qui bloque ce dossier ? »."
      "tags": 
        - "MEDICAL"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du visite médicale."
      "responses": 
        "200": 
          "description": "Circuit : étapes, étape courante, responsable, échéances, BLOCAGES"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/medical_visit/{id}/available-actions": 
    "get": 
      "operationId": "get_medical_visit_available_actions"
      "summary": "Ce que l'agent peut faire MAINTENANT sur cet objet, et pourquoi pas le reste"
      "description": "Ce que l'agent peut faire MAINTENANT sur cet objet, et pourquoi pas le reste"
      "tags": 
        - "MEDICAL"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du visite médicale."
      "responses": 
        "200": 
          "description": "Ce que l'agent peut faire MAINTENANT sur cet objet, et pourquoi pas le reste"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/medical_info_declaration": 
    "get": 
      "operationId": "list_medical_info_declaration"
      "summary": "Lister : Déclaration d'information médicale (PRIM)"
      "description": |-
          Déclaration soumise au pharmacien responsable puis à l'autorité : prise en charge, don, prestation — avec son circuit de validation et son ordre de paiement.
          
          Filtres possibles sur : id, reference, sourceType, sourceId, label, beneficiary, amount, status, requesterId, companyId, createdAt, authorityNotes. Syntaxe : `champ=valeur`, `champ=in:A,B`, `champ=gte:2026-01-01`, `champ=contains:texte`, `champ=null`.
      "tags": 
        - "MEDICAL_INFO"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "q"
          "in": "query"
          "schema": 
            "type": "string"
          "description": "Recherche plein texte sur : reference, label, beneficiary, authorityNotes."
        -           "name": "sort"
          "in": "query"
          "schema": 
            "type": "string"
          "description": "Tri « champ:asc » ou « champ:desc »."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Liste paginée de déclaration d'information médicale (prim)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/ListResult"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/medical_info_declaration/{id}": 
    "get": 
      "operationId": "get_medical_info_declaration"
      "summary": "Lire un déclaration d'information médicale (prim)"
      "description": "Déclaration soumise au pharmacien responsable puis à l'autorité : prise en charge, don, prestation — avec son circuit de validation et son ordre de paiement. Rend la fiche complète et les liens vers ses facettes (historique, pièces, circuit, actions)."
      "tags": 
        - "MEDICAL_INFO"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du déclaration d'information médicale (prim)."
      "responses": 
        "200": 
          "description": "Fiche complète."
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/medical_info_declaration/{id}/history": 
    "get": 
      "operationId": "get_medical_info_declaration_history"
      "summary": "Historique — actions humaines ET appels d'agents, distingués"
      "description": "Historique — actions humaines ET appels d'agents, distingués"
      "tags": 
        - "MEDICAL_INFO"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du déclaration d'information médicale (prim)."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Historique — actions humaines ET appels d'agents, distingués"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/medical_info_declaration/{id}/documents": 
    "get": 
      "operationId": "list_medical_info_declaration_documents"
      "summary": "Pièces jointes, avec leur chemin de téléchargement contrôlé"
      "description": "Pièces jointes, avec leur chemin de téléchargement contrôlé"
      "tags": 
        - "MEDICAL_INFO"
      "security": 
        -           "apiKey": 
            - "erp.documents.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du déclaration d'information médicale (prim)."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Pièces jointes, avec leur chemin de téléchargement contrôlé"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/medical_info_declaration/{id}/comments": 
    "get": 
      "operationId": "list_medical_info_declaration_comments"
      "summary": "Commentaires portés sur l'objet"
      "description": "Commentaires portés sur l'objet"
      "tags": 
        - "MEDICAL_INFO"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du déclaration d'information médicale (prim)."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Commentaires portés sur l'objet"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/medical_info_declaration/{id}/related": 
    "get": 
      "operationId": "get_medical_info_declaration_related"
      "summary": "Objets liés (sous-objets, lignes, participants)"
      "description": "Objets liés (sous-objets, lignes, participants)"
      "tags": 
        - "MEDICAL_INFO"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du déclaration d'information médicale (prim)."
      "responses": 
        "200": 
          "description": "Objets liés (sous-objets, lignes, participants)"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/medical_info_declaration/{id}/workflow": 
    "get": 
      "operationId": "get_medical_info_declaration_workflow"
      "summary": "Circuit : étapes, étape courante, responsable, échéances, BLOCAGES"
      "description": "Représentation structurée du circuit : étapes du workflow, celles déjà réalisées, l'étape courante, les prochaines possibles, le responsable, les dates et échéances, les pièces manquantes et les conditions à réunir pour avancer. C'est la réponse à « qu'est-ce qui bloque ce dossier ? »."
      "tags": 
        - "MEDICAL_INFO"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du déclaration d'information médicale (prim)."
      "responses": 
        "200": 
          "description": "Circuit : étapes, étape courante, responsable, échéances, BLOCAGES"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/medical_info_declaration/{id}/available-actions": 
    "get": 
      "operationId": "get_medical_info_declaration_available_actions"
      "summary": "Ce que l'agent peut faire MAINTENANT sur cet objet, et pourquoi pas le reste"
      "description": "Ce que l'agent peut faire MAINTENANT sur cet objet, et pourquoi pas le reste"
      "tags": 
        - "MEDICAL_INFO"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du déclaration d'information médicale (prim)."
      "responses": 
        "200": 
          "description": "Ce que l'agent peut faire MAINTENANT sur cet objet, et pourquoi pas le reste"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/sale": 
    "get": 
      "operationId": "list_sale"
      "summary": "Lister : Vente"
      "description": |-
          Vente de produit ou de service enregistrée par la force de vente.
          
          Filtres possibles sur : id, date, saleType, product, client, institution, quantity, unitPrice, revenue, paymentStatus, deliveryStatus, salesUserId, companyId, dci, comment. Syntaxe : `champ=valeur`, `champ=in:A,B`, `champ=gte:2026-01-01`, `champ=contains:texte`, `champ=null`.
      "tags": 
        - "SALES"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "q"
          "in": "query"
          "schema": 
            "type": "string"
          "description": "Recherche plein texte sur : product, client, institution, dci, comment."
        -           "name": "sort"
          "in": "query"
          "schema": 
            "type": "string"
          "description": "Tri « champ:asc » ou « champ:desc »."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Liste paginée de vente."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/ListResult"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/sale/{id}": 
    "get": 
      "operationId": "get_sale"
      "summary": "Lire un vente"
      "description": "Vente de produit ou de service enregistrée par la force de vente. Rend la fiche complète et les liens vers ses facettes (historique, pièces, circuit, actions)."
      "tags": 
        - "SALES"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du vente."
      "responses": 
        "200": 
          "description": "Fiche complète."
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/sale/{id}/history": 
    "get": 
      "operationId": "get_sale_history"
      "summary": "Historique — actions humaines ET appels d'agents, distingués"
      "description": "Historique — actions humaines ET appels d'agents, distingués"
      "tags": 
        - "SALES"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du vente."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Historique — actions humaines ET appels d'agents, distingués"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/sale/{id}/documents": 
    "get": 
      "operationId": "list_sale_documents"
      "summary": "Pièces jointes, avec leur chemin de téléchargement contrôlé"
      "description": "Pièces jointes, avec leur chemin de téléchargement contrôlé"
      "tags": 
        - "SALES"
      "security": 
        -           "apiKey": 
            - "erp.documents.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du vente."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Pièces jointes, avec leur chemin de téléchargement contrôlé"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/sale/{id}/comments": 
    "get": 
      "operationId": "list_sale_comments"
      "summary": "Commentaires portés sur l'objet"
      "description": "Commentaires portés sur l'objet"
      "tags": 
        - "SALES"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du vente."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Commentaires portés sur l'objet"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/sale/{id}/related": 
    "get": 
      "operationId": "get_sale_related"
      "summary": "Objets liés (sous-objets, lignes, participants)"
      "description": "Objets liés (sous-objets, lignes, participants)"
      "tags": 
        - "SALES"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du vente."
      "responses": 
        "200": 
          "description": "Objets liés (sous-objets, lignes, participants)"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/sale/{id}/workflow": 
    "get": 
      "operationId": "get_sale_workflow"
      "summary": "Circuit : étapes, étape courante, responsable, échéances, BLOCAGES"
      "description": "Représentation structurée du circuit : étapes du workflow, celles déjà réalisées, l'étape courante, les prochaines possibles, le responsable, les dates et échéances, les pièces manquantes et les conditions à réunir pour avancer. C'est la réponse à « qu'est-ce qui bloque ce dossier ? »."
      "tags": 
        - "SALES"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du vente."
      "responses": 
        "200": 
          "description": "Circuit : étapes, étape courante, responsable, échéances, BLOCAGES"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/sale/{id}/available-actions": 
    "get": 
      "operationId": "get_sale_available_actions"
      "summary": "Ce que l'agent peut faire MAINTENANT sur cet objet, et pourquoi pas le reste"
      "description": "Ce que l'agent peut faire MAINTENANT sur cet objet, et pourquoi pas le reste"
      "tags": 
        - "SALES"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du vente."
      "responses": 
        "200": 
          "description": "Ce que l'agent peut faire MAINTENANT sur cet objet, et pourquoi pas le reste"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/dossier": 
    "get": 
      "operationId": "list_dossier"
      "summary": "Lister : Dossier transverse"
      "description": |-
          Dossier de travail transverse (sujet suivi à plusieurs) avec ses participants, son fil de discussion et ses pièces.
          
          Filtres possibles sur : id, reference, title, category, priority, status, createdById, assignedToId, dueDate, companyId, description. Syntaxe : `champ=valeur`, `champ=in:A,B`, `champ=gte:2026-01-01`, `champ=contains:texte`, `champ=null`.
      "tags": 
        - "DOSSIERS"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "q"
          "in": "query"
          "schema": 
            "type": "string"
          "description": "Recherche plein texte sur : reference, title, description."
        -           "name": "sort"
          "in": "query"
          "schema": 
            "type": "string"
          "description": "Tri « champ:asc » ou « champ:desc »."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Liste paginée de dossier transverse."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/ListResult"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/dossier/{id}": 
    "get": 
      "operationId": "get_dossier"
      "summary": "Lire un dossier transverse"
      "description": "Dossier de travail transverse (sujet suivi à plusieurs) avec ses participants, son fil de discussion et ses pièces. Rend la fiche complète et les liens vers ses facettes (historique, pièces, circuit, actions)."
      "tags": 
        - "DOSSIERS"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du dossier transverse."
      "responses": 
        "200": 
          "description": "Fiche complète."
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/dossier/{id}/history": 
    "get": 
      "operationId": "get_dossier_history"
      "summary": "Historique — actions humaines ET appels d'agents, distingués"
      "description": "Historique — actions humaines ET appels d'agents, distingués"
      "tags": 
        - "DOSSIERS"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du dossier transverse."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Historique — actions humaines ET appels d'agents, distingués"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/dossier/{id}/documents": 
    "get": 
      "operationId": "list_dossier_documents"
      "summary": "Pièces jointes, avec leur chemin de téléchargement contrôlé"
      "description": "Pièces jointes, avec leur chemin de téléchargement contrôlé"
      "tags": 
        - "DOSSIERS"
      "security": 
        -           "apiKey": 
            - "erp.documents.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du dossier transverse."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Pièces jointes, avec leur chemin de téléchargement contrôlé"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/dossier/{id}/comments": 
    "get": 
      "operationId": "list_dossier_comments"
      "summary": "Commentaires portés sur l'objet"
      "description": "Commentaires portés sur l'objet"
      "tags": 
        - "DOSSIERS"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du dossier transverse."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Commentaires portés sur l'objet"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/dossier/{id}/related": 
    "get": 
      "operationId": "get_dossier_related"
      "summary": "Objets liés (sous-objets, lignes, participants)"
      "description": "Objets liés (sous-objets, lignes, participants)"
      "tags": 
        - "DOSSIERS"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du dossier transverse."
      "responses": 
        "200": 
          "description": "Objets liés (sous-objets, lignes, participants)"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/dossier/{id}/workflow": 
    "get": 
      "operationId": "get_dossier_workflow"
      "summary": "Circuit : étapes, étape courante, responsable, échéances, BLOCAGES"
      "description": "Représentation structurée du circuit : étapes du workflow, celles déjà réalisées, l'étape courante, les prochaines possibles, le responsable, les dates et échéances, les pièces manquantes et les conditions à réunir pour avancer. C'est la réponse à « qu'est-ce qui bloque ce dossier ? »."
      "tags": 
        - "DOSSIERS"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du dossier transverse."
      "responses": 
        "200": 
          "description": "Circuit : étapes, étape courante, responsable, échéances, BLOCAGES"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/dossier/{id}/available-actions": 
    "get": 
      "operationId": "get_dossier_available_actions"
      "summary": "Ce que l'agent peut faire MAINTENANT sur cet objet, et pourquoi pas le reste"
      "description": "Ce que l'agent peut faire MAINTENANT sur cet objet, et pourquoi pas le reste"
      "tags": 
        - "DOSSIERS"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du dossier transverse."
      "responses": 
        "200": 
          "description": "Ce que l'agent peut faire MAINTENANT sur cet objet, et pourquoi pas le reste"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/task": 
    "get": 
      "operationId": "list_task"
      "summary": "Lister : Tâche"
      "description": |-
          Tâche assignée à une personne, éventuellement rattachée à un projet ou à un objet métier.
          
          Filtres possibles sur : id, title, status, priority, assignedToId, createdById, dueDate, module, relatedEntityType, relatedEntityId, createdAt, description. Syntaxe : `champ=valeur`, `champ=in:A,B`, `champ=gte:2026-01-01`, `champ=contains:texte`, `champ=null`.
      "tags": 
        - "WORKSPACE"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "q"
          "in": "query"
          "schema": 
            "type": "string"
          "description": "Recherche plein texte sur : title, description."
        -           "name": "sort"
          "in": "query"
          "schema": 
            "type": "string"
          "description": "Tri « champ:asc » ou « champ:desc »."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Liste paginée de tâche."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/ListResult"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/task/{id}": 
    "get": 
      "operationId": "get_task"
      "summary": "Lire un tâche"
      "description": "Tâche assignée à une personne, éventuellement rattachée à un projet ou à un objet métier. Rend la fiche complète et les liens vers ses facettes (historique, pièces, circuit, actions)."
      "tags": 
        - "WORKSPACE"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du tâche."
      "responses": 
        "200": 
          "description": "Fiche complète."
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/task/{id}/history": 
    "get": 
      "operationId": "get_task_history"
      "summary": "Historique — actions humaines ET appels d'agents, distingués"
      "description": "Historique — actions humaines ET appels d'agents, distingués"
      "tags": 
        - "WORKSPACE"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du tâche."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Historique — actions humaines ET appels d'agents, distingués"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/task/{id}/documents": 
    "get": 
      "operationId": "list_task_documents"
      "summary": "Pièces jointes, avec leur chemin de téléchargement contrôlé"
      "description": "Pièces jointes, avec leur chemin de téléchargement contrôlé"
      "tags": 
        - "WORKSPACE"
      "security": 
        -           "apiKey": 
            - "erp.documents.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du tâche."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Pièces jointes, avec leur chemin de téléchargement contrôlé"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/task/{id}/comments": 
    "get": 
      "operationId": "list_task_comments"
      "summary": "Commentaires portés sur l'objet"
      "description": "Commentaires portés sur l'objet"
      "tags": 
        - "WORKSPACE"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du tâche."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Commentaires portés sur l'objet"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/task/{id}/related": 
    "get": 
      "operationId": "get_task_related"
      "summary": "Objets liés (sous-objets, lignes, participants)"
      "description": "Objets liés (sous-objets, lignes, participants)"
      "tags": 
        - "WORKSPACE"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du tâche."
      "responses": 
        "200": 
          "description": "Objets liés (sous-objets, lignes, participants)"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/task/{id}/workflow": 
    "get": 
      "operationId": "get_task_workflow"
      "summary": "Circuit : étapes, étape courante, responsable, échéances, BLOCAGES"
      "description": "Représentation structurée du circuit : étapes du workflow, celles déjà réalisées, l'étape courante, les prochaines possibles, le responsable, les dates et échéances, les pièces manquantes et les conditions à réunir pour avancer. C'est la réponse à « qu'est-ce qui bloque ce dossier ? »."
      "tags": 
        - "WORKSPACE"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du tâche."
      "responses": 
        "200": 
          "description": "Circuit : étapes, étape courante, responsable, échéances, BLOCAGES"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/task/{id}/available-actions": 
    "get": 
      "operationId": "get_task_available_actions"
      "summary": "Ce que l'agent peut faire MAINTENANT sur cet objet, et pourquoi pas le reste"
      "description": "Ce que l'agent peut faire MAINTENANT sur cet objet, et pourquoi pas le reste"
      "tags": 
        - "WORKSPACE"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du tâche."
      "responses": 
        "200": 
          "description": "Ce que l'agent peut faire MAINTENANT sur cet objet, et pourquoi pas le reste"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/document": 
    "get": 
      "operationId": "list_document"
      "summary": "Lister : Document"
      "description": |-
          Pièce jointe rattachée à un objet métier (facture, scan, rapport). Le contenu se télécharge par un point d'accès dédié, jamais par un chemin de fichier.
          
          Filtres possibles sur : id, name, category, entityType, entityId, stepKey, mimeType, sizeBytes, version, confidentiality, uploadedById, createdAt. Syntaxe : `champ=valeur`, `champ=in:A,B`, `champ=gte:2026-01-01`, `champ=contains:texte`, `champ=null`.
      "tags": 
        - "DOCUMENTS"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "q"
          "in": "query"
          "schema": 
            "type": "string"
          "description": "Recherche plein texte sur : name."
        -           "name": "sort"
          "in": "query"
          "schema": 
            "type": "string"
          "description": "Tri « champ:asc » ou « champ:desc »."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Liste paginée de document."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/ListResult"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/document/{id}": 
    "get": 
      "operationId": "get_document"
      "summary": "Lire un document"
      "description": "Pièce jointe rattachée à un objet métier (facture, scan, rapport). Le contenu se télécharge par un point d'accès dédié, jamais par un chemin de fichier. Rend la fiche complète et les liens vers ses facettes (historique, pièces, circuit, actions)."
      "tags": 
        - "DOCUMENTS"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du document."
      "responses": 
        "200": 
          "description": "Fiche complète."
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/document/{id}/history": 
    "get": 
      "operationId": "get_document_history"
      "summary": "Historique — actions humaines ET appels d'agents, distingués"
      "description": "Historique — actions humaines ET appels d'agents, distingués"
      "tags": 
        - "DOCUMENTS"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du document."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Historique — actions humaines ET appels d'agents, distingués"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/document/{id}/documents": 
    "get": 
      "operationId": "list_document_documents"
      "summary": "Pièces jointes, avec leur chemin de téléchargement contrôlé"
      "description": "Pièces jointes, avec leur chemin de téléchargement contrôlé"
      "tags": 
        - "DOCUMENTS"
      "security": 
        -           "apiKey": 
            - "erp.documents.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du document."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Pièces jointes, avec leur chemin de téléchargement contrôlé"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/document/{id}/comments": 
    "get": 
      "operationId": "list_document_comments"
      "summary": "Commentaires portés sur l'objet"
      "description": "Commentaires portés sur l'objet"
      "tags": 
        - "DOCUMENTS"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du document."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Commentaires portés sur l'objet"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/document/{id}/related": 
    "get": 
      "operationId": "get_document_related"
      "summary": "Objets liés (sous-objets, lignes, participants)"
      "description": "Objets liés (sous-objets, lignes, participants)"
      "tags": 
        - "DOCUMENTS"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du document."
      "responses": 
        "200": 
          "description": "Objets liés (sous-objets, lignes, participants)"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/document/{id}/workflow": 
    "get": 
      "operationId": "get_document_workflow"
      "summary": "Circuit : étapes, étape courante, responsable, échéances, BLOCAGES"
      "description": "Représentation structurée du circuit : étapes du workflow, celles déjà réalisées, l'étape courante, les prochaines possibles, le responsable, les dates et échéances, les pièces manquantes et les conditions à réunir pour avancer. C'est la réponse à « qu'est-ce qui bloque ce dossier ? »."
      "tags": 
        - "DOCUMENTS"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du document."
      "responses": 
        "200": 
          "description": "Circuit : étapes, étape courante, responsable, échéances, BLOCAGES"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/document/{id}/available-actions": 
    "get": 
      "operationId": "get_document_available_actions"
      "summary": "Ce que l'agent peut faire MAINTENANT sur cet objet, et pourquoi pas le reste"
      "description": "Ce que l'agent peut faire MAINTENANT sur cet objet, et pourquoi pas le reste"
      "tags": 
        - "DOCUMENTS"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du document."
      "responses": 
        "200": 
          "description": "Ce que l'agent peut faire MAINTENANT sur cet objet, et pourquoi pas le reste"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/user": 
    "get": 
      "operationId": "list_user"
      "summary": "Lister : Compte utilisateur"
      "description": |-
          Compte de connexion, son rôle et ses droits. Objet d'administration : l'exposer demande la portée `erp.admin`.
          
          Filtres possibles sur : id, name, email, role, secondaryRole, isActive, departmentId, title, lastLoginAt, createdAt. Syntaxe : `champ=valeur`, `champ=in:A,B`, `champ=gte:2026-01-01`, `champ=contains:texte`, `champ=null`.
      "tags": 
        - "ADMIN"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "q"
          "in": "query"
          "schema": 
            "type": "string"
          "description": "Recherche plein texte sur : name, email."
        -           "name": "sort"
          "in": "query"
          "schema": 
            "type": "string"
          "description": "Tri « champ:asc » ou « champ:desc »."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Liste paginée de compte utilisateur."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/ListResult"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/user/{id}": 
    "get": 
      "operationId": "get_user"
      "summary": "Lire un compte utilisateur"
      "description": "Compte de connexion, son rôle et ses droits. Objet d'administration : l'exposer demande la portée `erp.admin`. Rend la fiche complète et les liens vers ses facettes (historique, pièces, circuit, actions)."
      "tags": 
        - "ADMIN"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du compte utilisateur."
      "responses": 
        "200": 
          "description": "Fiche complète."
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/user/{id}/history": 
    "get": 
      "operationId": "get_user_history"
      "summary": "Historique — actions humaines ET appels d'agents, distingués"
      "description": "Historique — actions humaines ET appels d'agents, distingués"
      "tags": 
        - "ADMIN"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du compte utilisateur."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Historique — actions humaines ET appels d'agents, distingués"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/user/{id}/documents": 
    "get": 
      "operationId": "list_user_documents"
      "summary": "Pièces jointes, avec leur chemin de téléchargement contrôlé"
      "description": "Pièces jointes, avec leur chemin de téléchargement contrôlé"
      "tags": 
        - "ADMIN"
      "security": 
        -           "apiKey": 
            - "erp.documents.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du compte utilisateur."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Pièces jointes, avec leur chemin de téléchargement contrôlé"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/user/{id}/comments": 
    "get": 
      "operationId": "list_user_comments"
      "summary": "Commentaires portés sur l'objet"
      "description": "Commentaires portés sur l'objet"
      "tags": 
        - "ADMIN"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du compte utilisateur."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Commentaires portés sur l'objet"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/user/{id}/related": 
    "get": 
      "operationId": "get_user_related"
      "summary": "Objets liés (sous-objets, lignes, participants)"
      "description": "Objets liés (sous-objets, lignes, participants)"
      "tags": 
        - "ADMIN"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du compte utilisateur."
      "responses": 
        "200": 
          "description": "Objets liés (sous-objets, lignes, participants)"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/user/{id}/workflow": 
    "get": 
      "operationId": "get_user_workflow"
      "summary": "Circuit : étapes, étape courante, responsable, échéances, BLOCAGES"
      "description": "Représentation structurée du circuit : étapes du workflow, celles déjà réalisées, l'étape courante, les prochaines possibles, le responsable, les dates et échéances, les pièces manquantes et les conditions à réunir pour avancer. C'est la réponse à « qu'est-ce qui bloque ce dossier ? »."
      "tags": 
        - "ADMIN"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du compte utilisateur."
      "responses": 
        "200": 
          "description": "Circuit : étapes, étape courante, responsable, échéances, BLOCAGES"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/user/{id}/available-actions": 
    "get": 
      "operationId": "get_user_available_actions"
      "summary": "Ce que l'agent peut faire MAINTENANT sur cet objet, et pourquoi pas le reste"
      "description": "Ce que l'agent peut faire MAINTENANT sur cet objet, et pourquoi pas le reste"
      "tags": 
        - "ADMIN"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du compte utilisateur."
      "responses": 
        "200": 
          "description": "Ce que l'agent peut faire MAINTENANT sur cet objet, et pourquoi pas le reste"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/audit_log": 
    "get": 
      "operationId": "list_audit_log"
      "summary": "Lister : Entrée du journal d'audit"
      "description": |-
          Trace d'une action humaine dans l'ERP : qui, quoi, sur quel objet, quand.
          
          Filtres possibles sur : id, actorId, action, module, entityType, entityId, field, oldValue, newValue, summary, createdAt. Syntaxe : `champ=valeur`, `champ=in:A,B`, `champ=gte:2026-01-01`, `champ=contains:texte`, `champ=null`.
      "tags": 
        - "ADMIN"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "q"
          "in": "query"
          "schema": 
            "type": "string"
          "description": "Recherche plein texte sur : summary."
        -           "name": "sort"
          "in": "query"
          "schema": 
            "type": "string"
          "description": "Tri « champ:asc » ou « champ:desc »."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Liste paginée de entrée du journal d'audit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/ListResult"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/audit_log/{id}": 
    "get": 
      "operationId": "get_audit_log"
      "summary": "Lire un entrée du journal d'audit"
      "description": "Trace d'une action humaine dans l'ERP : qui, quoi, sur quel objet, quand. Rend la fiche complète et les liens vers ses facettes (historique, pièces, circuit, actions)."
      "tags": 
        - "ADMIN"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du entrée du journal d'audit."
      "responses": 
        "200": 
          "description": "Fiche complète."
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/audit_log/{id}/history": 
    "get": 
      "operationId": "get_audit_log_history"
      "summary": "Historique — actions humaines ET appels d'agents, distingués"
      "description": "Historique — actions humaines ET appels d'agents, distingués"
      "tags": 
        - "ADMIN"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du entrée du journal d'audit."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Historique — actions humaines ET appels d'agents, distingués"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/audit_log/{id}/documents": 
    "get": 
      "operationId": "list_audit_log_documents"
      "summary": "Pièces jointes, avec leur chemin de téléchargement contrôlé"
      "description": "Pièces jointes, avec leur chemin de téléchargement contrôlé"
      "tags": 
        - "ADMIN"
      "security": 
        -           "apiKey": 
            - "erp.documents.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du entrée du journal d'audit."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Pièces jointes, avec leur chemin de téléchargement contrôlé"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/audit_log/{id}/comments": 
    "get": 
      "operationId": "list_audit_log_comments"
      "summary": "Commentaires portés sur l'objet"
      "description": "Commentaires portés sur l'objet"
      "tags": 
        - "ADMIN"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du entrée du journal d'audit."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Commentaires portés sur l'objet"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/audit_log/{id}/related": 
    "get": 
      "operationId": "get_audit_log_related"
      "summary": "Objets liés (sous-objets, lignes, participants)"
      "description": "Objets liés (sous-objets, lignes, participants)"
      "tags": 
        - "ADMIN"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du entrée du journal d'audit."
      "responses": 
        "200": 
          "description": "Objets liés (sous-objets, lignes, participants)"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/audit_log/{id}/workflow": 
    "get": 
      "operationId": "get_audit_log_workflow"
      "summary": "Circuit : étapes, étape courante, responsable, échéances, BLOCAGES"
      "description": "Représentation structurée du circuit : étapes du workflow, celles déjà réalisées, l'étape courante, les prochaines possibles, le responsable, les dates et échéances, les pièces manquantes et les conditions à réunir pour avancer. C'est la réponse à « qu'est-ce qui bloque ce dossier ? »."
      "tags": 
        - "ADMIN"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du entrée du journal d'audit."
      "responses": 
        "200": 
          "description": "Circuit : étapes, étape courante, responsable, échéances, BLOCAGES"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/audit_log/{id}/available-actions": 
    "get": 
      "operationId": "get_audit_log_available_actions"
      "summary": "Ce que l'agent peut faire MAINTENANT sur cet objet, et pourquoi pas le reste"
      "description": "Ce que l'agent peut faire MAINTENANT sur cet objet, et pourquoi pas le reste"
      "tags": 
        - "ADMIN"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du entrée du journal d'audit."
      "responses": 
        "200": 
          "description": "Ce que l'agent peut faire MAINTENANT sur cet objet, et pourquoi pas le reste"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/notification": 
    "get": 
      "operationId": "list_notification"
      "summary": "Lister : Notification"
      "description": |-
          Alerte adressée à une personne (assignation, validation attendue, échéance).
          
          Filtres possibles sur : id, userId, type, title, body, link, isRead, createdAt. Syntaxe : `champ=valeur`, `champ=in:A,B`, `champ=gte:2026-01-01`, `champ=contains:texte`, `champ=null`.
      "tags": 
        - "NOTIFICATIONS"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "q"
          "in": "query"
          "schema": 
            "type": "string"
          "description": "Recherche plein texte sur : title, body."
        -           "name": "sort"
          "in": "query"
          "schema": 
            "type": "string"
          "description": "Tri « champ:asc » ou « champ:desc »."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Liste paginée de notification."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/ListResult"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/notification/{id}": 
    "get": 
      "operationId": "get_notification"
      "summary": "Lire un notification"
      "description": "Alerte adressée à une personne (assignation, validation attendue, échéance). Rend la fiche complète et les liens vers ses facettes (historique, pièces, circuit, actions)."
      "tags": 
        - "NOTIFICATIONS"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du notification."
      "responses": 
        "200": 
          "description": "Fiche complète."
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/notification/{id}/history": 
    "get": 
      "operationId": "get_notification_history"
      "summary": "Historique — actions humaines ET appels d'agents, distingués"
      "description": "Historique — actions humaines ET appels d'agents, distingués"
      "tags": 
        - "NOTIFICATIONS"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du notification."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Historique — actions humaines ET appels d'agents, distingués"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/notification/{id}/documents": 
    "get": 
      "operationId": "list_notification_documents"
      "summary": "Pièces jointes, avec leur chemin de téléchargement contrôlé"
      "description": "Pièces jointes, avec leur chemin de téléchargement contrôlé"
      "tags": 
        - "NOTIFICATIONS"
      "security": 
        -           "apiKey": 
            - "erp.documents.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du notification."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Pièces jointes, avec leur chemin de téléchargement contrôlé"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/notification/{id}/comments": 
    "get": 
      "operationId": "list_notification_comments"
      "summary": "Commentaires portés sur l'objet"
      "description": "Commentaires portés sur l'objet"
      "tags": 
        - "NOTIFICATIONS"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du notification."
        -           "name": "limit"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 1
            "maximum": 500
            "default": 50
          "description": "Taille du lot (max 500)."
        -           "name": "offset"
          "in": "query"
          "schema": 
            "type": "integer"
            "minimum": 0
            "default": 0
          "description": "Décalage pour la page suivante."
      "responses": 
        "200": 
          "description": "Commentaires portés sur l'objet"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/notification/{id}/related": 
    "get": 
      "operationId": "get_notification_related"
      "summary": "Objets liés (sous-objets, lignes, participants)"
      "description": "Objets liés (sous-objets, lignes, participants)"
      "tags": 
        - "NOTIFICATIONS"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du notification."
      "responses": 
        "200": 
          "description": "Objets liés (sous-objets, lignes, participants)"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/notification/{id}/workflow": 
    "get": 
      "operationId": "get_notification_workflow"
      "summary": "Circuit : étapes, étape courante, responsable, échéances, BLOCAGES"
      "description": "Représentation structurée du circuit : étapes du workflow, celles déjà réalisées, l'étape courante, les prochaines possibles, le responsable, les dates et échéances, les pièces manquantes et les conditions à réunir pour avancer. C'est la réponse à « qu'est-ce qui bloque ce dossier ? »."
      "tags": 
        - "NOTIFICATIONS"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du notification."
      "responses": 
        "200": 
          "description": "Circuit : étapes, étape courante, responsable, échéances, BLOCAGES"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/entities/notification/{id}/available-actions": 
    "get": 
      "operationId": "get_notification_available_actions"
      "summary": "Ce que l'agent peut faire MAINTENANT sur cet objet, et pourquoi pas le reste"
      "description": "Ce que l'agent peut faire MAINTENANT sur cet objet, et pourquoi pas le reste"
      "tags": 
        - "NOTIFICATIONS"
      "security": 
        -           "apiKey": 
            - "erp.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
          "description": "Identifiant du notification."
      "responses": 
        "200": 
          "description": "Ce que l'agent peut faire MAINTENANT sur cet objet, et pourquoi pas le reste"
          "content": 
            "application/json": 
              "schema": 
                "type": "object"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
  "/api/v1/documents/{id}/content": 
    "get": 
      "operationId": "download_document_content"
      "summary": "Télécharger le contenu d'une pièce jointe"
      "description": "Le contenu binaire, par identifiant. Aucun chemin de fichier interne n'est jamais exposé : l'accès est recroisé avec les droits de l'identité sur l'objet auquel la pièce est rattachée."
      "tags": 
        - "Documents"
      "security": 
        -           "apiKey": 
            - "erp.documents.read"
      "parameters": 
        -           "name": "id"
          "in": "path"
          "required": true
          "schema": 
            "type": "string"
      "responses": 
        "200": 
          "description": "Contenu du fichier."
          "content": 
            "application/octet-stream": 
              "schema": 
                "type": "string"
                "format": "binary"
        "401": 
          "description": "Clé absente, inconnue ou expirée."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "403": 
          "description": "Portée insuffisante, ou droit refusé à l'identité au nom de laquelle l'agent agit."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "404": 
          "description": "Objet introuvable — ou hors de la portée de cette identité."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "422": 
          "description": "Requête invalide (filtre inconnu, corps illisible…)."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
        "500": 
          "description": "Erreur interne."
          "content": 
            "application/json": 
              "schema": 
                "$ref": "#/components/schemas/Error"
"tags": 
  -     "name": "Découverte"
    "description": "Découvrir la structure de l'ERP sans documentation écrite."
  -     "name": "Recherche"
    "description": "Recherche globale transverse."
  -     "name": "Documents"
    "description": "Pièces jointes et téléchargement contrôlé."
  -     "name": "REGULATORY"
    "description": "Objets du module REGULATORY."
  -     "name": "DASHBOARD"
    "description": "Objets du module DASHBOARD."
  -     "name": "RH"
    "description": "Objets du module RH."
  -     "name": "ADMIN_REQUESTS"
    "description": "Objets du module ADMIN_REQUESTS."
  -     "name": "SPONSORING"
    "description": "Objets du module SPONSORING."
  -     "name": "CONGRESS_INTERNATIONAL"
    "description": "Objets du module CONGRESS_INTERNATIONAL."
  -     "name": "CONGRESS_NATIONAL"
    "description": "Objets du module CONGRESS_NATIONAL."
  -     "name": "EVENTS"
    "description": "Objets du module EVENTS."
  -     "name": "FINANCES"
    "description": "Objets du module FINANCES."
  -     "name": "BUDGETS"
    "description": "Objets du module BUDGETS."
  -     "name": "STOCKS"
    "description": "Objets du module STOCKS."
  -     "name": "PCH"
    "description": "Objets du module PCH."
  -     "name": "MEDICAL"
    "description": "Objets du module MEDICAL."
  -     "name": "MEDICAL_INFO"
    "description": "Objets du module MEDICAL_INFO."
  -     "name": "SALES"
    "description": "Objets du module SALES."
  -     "name": "DOSSIERS"
    "description": "Objets du module DOSSIERS."
  -     "name": "WORKSPACE"
    "description": "Objets du module WORKSPACE."
  -     "name": "DOCUMENTS"
    "description": "Objets du module DOCUMENTS."
  -     "name": "ADMIN"
    "description": "Objets du module ADMIN."
  -     "name": "NOTIFICATIONS"
    "description": "Objets du module NOTIFICATIONS."
"components": 
  "securitySchemes": 
    "apiKey": 
      "type": "http"
      "scheme": "bearer"
      "description": |-
          Clé machine : `Authorization: Bearer amd_sk_…`. Émise par l'administration, rattachée à une identité et à des portées, révocable indépendamment des comptes humains.
          
          Portées : `erp.read` — Lire les objets, leurs relations, leur historique et leurs workflows. Aucune écriture. · `erp.search` — Interroger la recherche globale et les listes filtrées. · `erp.write` — Créer et modifier des objets métier (hors validation et administration). · `erp.documents.read` — Lister les pièces jointes et télécharger leur contenu. · `erp.documents.write` — Téléverser une pièce jointe et la rattacher à un objet. · `erp.workflow.execute` — Faire avancer un circuit : soumettre, transmettre, exécuter une étape. · `erp.approve` — Décider une validation (approuver / refuser). Séparé de `erp.write` à dessein. · `erp.admin` — Administration : comptes, droits, réglages, purge. À n'accorder qu'exceptionnellement.
          
          Profil LECTURE SEULE recommandé : erp.read, erp.search, erp.documents.read.
  "schemas": 
    "Error": 
      "type": "object"
      "required": 
        - "error"
      "properties": 
        "error": 
          "type": "object"
          "required": 
            - "code"
            - "message"
          "properties": 
            "code": 
              "type": "string"
              "enum": 
                - "UNAUTHENTICATED"
                - "INVALID_KEY"
                - "KEY_EXPIRED"
                - "CLIENT_DISABLED"
                - "MISSING_SCOPE"
                - "FORBIDDEN"
                - "NOT_FOUND"
                - "METHOD_NOT_ALLOWED"
                - "CONFLICT"
                - "IDEMPOTENCY_MISMATCH"
                - "VERSION_CONFLICT"
                - "VALIDATION_FAILED"
                - "RATE_LIMITED"
                - "INTERNAL"
              "description": "Code stable — c'est lui qu'un agent doit tester, pas le message."
            "message": 
              "type": "string"
            "hint": 
              "type": "string"
              "description": "Ce qu'il faut faire pour lever l'erreur."
            "requiredScopes": 
              "type": "array"
              "items": 
                "type": "string"
            "fields": 
              "type": "object"
              "additionalProperties": 
                "type": "string"
            "correlationId": 
              "type": "string"
              "description": "À citer pour retrouver l'appel exact dans le journal."
    "ListResult": 
      "type": "object"
      "required": 
        - "items"
        - "page"
      "properties": 
        "items": 
          "type": "array"
          "items": 
            "type": "object"
        "page": 
          "type": "object"
          "properties": 
            "limit": 
              "type": "integer"
            "offset": 
              "type": "integer"
            "total": 
              "type": "integer"
              "description": "Total DANS LA PORTÉE de l'identité — jamais le total absolu."
            "hasMore": 
              "type": "boolean"
"security": 
  -     "apiKey": []
